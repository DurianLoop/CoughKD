# 5.112 Semantic router citation and paper-quality pass

## Purpose

5.111 integrated the semantic-constrained transfer router into the ICASSP draft.

This note records a targeted quality pass: citation coverage, novelty-boundary wording, BibTeX refresh, compile status, and page distribution.

## Files changed

- `paper_icassp2027/main.tex`
- `paper/references.bib`

## Citation additions

Added references:

- Symptom-assisted cough modeling:
  - `lin2025sympcoughnet`
  - SympCoughNet, Frontiers in Digital Health 2025
  - https://www.frontiersin.org/journals/digital-health/articles/10.3389/fdgth.2025.1551298/full
- Negative-transfer survey:
  - `zhang2022negativetransfer`
  - IEEE/CAA Journal of Automatica Sinica 2023
  - https://www.ieee-jas.net/article/doi/10.1109/JAS.2022.106004
- Semantic descriptors for domain adaptation:
  - `yang2015unified`
  - ICLR 2015 / arXiv 1412.7489
  - https://arxiv.org/abs/1412.7489
- Zero-shot domain adaptation without descriptors:
  - `kumagai2018zeroshot`
  - arXiv 1807.02927
  - https://arxiv.org/abs/1807.02927

## Novelty-boundary wording added

The introduction now explicitly says the paper is not claiming:

- first metadata model;
- first safe-transfer method;
- first KD method;
- first symptom-assisted cough model.

The semantic-router section now also says the router is not:

- label-free;
- a general claim that metadata helps;
- semantic descriptor learning for unseen domains.

This makes the remaining claim narrower:

> target metadata semantics can rescue transfer routing when unlabeled guards and generic slice selection fail.

## Build commands

```cmd
cd D:\CoughKD\AAAI\paper_icassp2027
pdflatex -interaction=nonstopmode main.tex
bibtex main
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
```

`bibtex` and `pdftotext` both emitted a MiKTeX user-log permission message, but completed successfully and updated their target files.

## Compile status

Final log scan:

- no `Undefined`;
- no `Warning`;
- no `Error`;
- no `Overfull`;
- output: `main.pdf (5 pages, 231762 bytes)`.

Only underfull box messages remain during compile.

## PDF text checks

`main_pdftotext.txt` contains:

- semantic-router abstract claim;
- novelty-boundary sentence:
  - not first metadata model;
  - not first safe-transfer method;
  - not first KD method;
- added references:
  - SympCoughNet;
  - negative transfer;
  - semantic descriptors.

Page distribution from form-feed split:

| page | extracted chars | notes |
| ---: | ---: | --- |
| 1 | 4476 | abstract/introduction/method start |
| 2 | 5068 | semantic-router method and setup |
| 3 | 5436 | results and semantic tables |
| 4 | 5033 | discussion/conclusion plus references start |
| 5 | 2257 | references |

Interpretation:

- The draft remains 5 pages including references.
- References start on page 4, so the technical body still fits roughly within a 4-page body shape.
- Final ICASSP 2027 formatting rules must still be checked before submission.

## Current claim status

Stronger than 5.111:

- novelty boundaries are now explicitly guarded by citations;
- semantic-router claim is positioned against symptom-assisted cough modeling, negative transfer, and semantic descriptor adaptation;
- compile artifacts are clean.

Still not final:

- "nobody has done this exact thing" is not fully certified;
- only two large external targets are used;
- UKCOVID/Cambridge/HeAR would still be the best path to a stronger ICASSP evidence base;
- the paper may need compression after final format rules are confirmed.

## Next action

Continue targeted novelty search specifically for:

- metadata-conditioned model selection;
- metadata type safety;
- demographic-vs-symptom routing;
- safe transfer routing in medical audio.

If no direct collision appears, the draft can move from "promising internal claim" toward "candidate ICASSP claim," with the caveat that third-target validation remains highly desirable.

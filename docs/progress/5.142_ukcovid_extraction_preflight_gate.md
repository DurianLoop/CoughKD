# 5.142 UKCOVID Extraction Preflight Gate

Date: 2026-06-09

## Purpose

The UKCOVID route already had no-download preflight, resumable download, and
archive checksum gates. I added a small extraction preflight so the workflow can
check the manual extraction boundary before spending compute on onboarding or
semantic-router experiments.

## New script

- `scripts/check_ukcovid_extraction_preflight.py`

The script checks:

1. archive integrity verdict from `ukcovid_audio_archive_integrity.json`;
2. presence of `D:\CoughKD\external\ukcovid_open\covid_data.zip`;
3. whether 7-Zip is discoverable on PATH, under Program Files, or under
   `D:\CoughKD\tools\7zip`;
4. the exact extraction command when extraction is safe to run.

Current verdict:

```text
ARCHIVE_NOT_READY
```

This is expected because the UKCOVID split archive has not been downloaded.

## Integration

- `scripts/run_ukcovid_audio_download_verify_windows.cmd` now has step 6:
  extraction preflight.
- `runs/semantic_router_third_target_execution_pack/UKCOVID_SEMANTIC_ROUTER_EXECUTION_PACK.md`
  now requires `READY_TO_EXTRACT` before manual extraction.
- `scripts/audit_semantic_router_submission_readiness.py` checks the extraction
  preflight artifacts.
- `scripts/build_semantic_router_claim_dossier.py` includes the extraction
  preflight verdict in the third-target execution-plan requirement.

## Validation

Commands run:

```text
D:\conda\envs\CoughKD\python.exe -B scripts\check_ukcovid_extraction_preflight.py
D:\conda\envs\CoughKD\python.exe -B -c "from pathlib import Path; files=['scripts/check_ukcovid_extraction_preflight.py','scripts/audit_semantic_router_submission_readiness.py','scripts/build_semantic_router_claim_dossier.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('compile_ok', len(files))"
cmd /c scripts\run_ukcovid_audio_download_verify_windows.cmd --dry-run
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
D:\conda\envs\CoughKD\python.exe -B scripts\build_semantic_router_claim_dossier.py
```

Result:

- Extraction preflight report written to
  `runs/ukcovid_open_metadata_audit/UKCOVID_EXTRACTION_PREFLIGHT.md`.
- Compile check passed without writing pyc files.
- Download driver dry-run completed without starting a download.
- Submission readiness remains `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`.
- Claim dossier remains `CREDIBLE_CANDIDATE_NOT_FINAL_READY`.
- Remaining hard blocker is unchanged: UKCOVID audio download/extraction,
  onboarding, and subject-grouped third-target validation.

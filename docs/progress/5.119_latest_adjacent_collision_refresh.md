# 5.119 Latest adjacent collision refresh

## Purpose

Continue the long-running ICASSP 2027 claim-hardening loop with a low-cost novelty refresh.

The target claim remains:

> Target metadata semantics can act as a safety prior for transfer routing under cough dataset shift.

## Local state checked

Reviewed the current claim and evidence anchors in:

- `paper_icassp2027/main.tex`
- `docs/progress/5.110_focused_novelty_audit_and_paper_tables.md`
- `docs/progress/5.113_targeted_collision_search_and_multimodal_metadata_anchor.md`
- `docs/progress/5.114_alternative_terminology_collision_audit.md`
- `docs/progress/5.116_claim_readiness_and_controller_comparison.md`
- `docs/progress/5.118_final_compression_and_page_rule_check.md`

Current paper wording is already narrow:

- not a first metadata model;
- not a first symptom model;
- not metadata-gated representation adaptation;
- not a general safe-transfer or KD method;
- positive unit is a semantic safety prior over metadata field type for choosing between slice gating and calibration stacking.

## External novelty refresh

Targeted query families:

- `"metadata field" "safe transfer" "domain adaptation"`
- `"metadata semantics" "transfer routing"`
- `"clinical metadata" "model selection" "domain adaptation"`
- `"context-aware" "transfer learning" "model selection" metadata`
- `"CoughSense" "Coswara" "COUGHVID" "Virufy"`
- `"CoughSense" "FiLM" symptoms domain adaptation arXiv`

## New important adjacent work

Added:

- Nikhil Vincent, "CoughSense: Five-Class Respiratory Disease Classification via Whisper Encoder Fine-Tuning and Dual-Encoder Cross-Attention Fusion with Balanced Contrastive Learning", arXiv:2606.02998, 2026.
- Link: `https://arxiv.org/abs/2606.02998`
- Bib key: `vincent2026coughsense`

Why it matters:

- It is very recent.
- It is cough/respiratory audio.
- It involves the same broad public-dataset ecosystem: Coswara, COUGHVID, and Virufy.
- It is close to symptom-aware and domain-adaptive respiratory modeling.

Why it is not a direct collision:

- It is not a deployment-time router between pre-existing transfer strategies.
- It does not appear to classify metadata fields as semantically safe or unsafe routing variables.
- It does not appear to route symptom/acquisition metadata to slice gates while routing demographic-only metadata to calibration stacking.
- It does not appear to use inverted, all-slice, and no-slice semantic controls showing collapse when metadata semantics are flipped.

Impact on claim boundary:

- The paper must not imply novelty for symptom conditioning, metadata use, domain adaptation, Whisper-based cough modeling, or multi-dataset cough classification.
- The semantic-router claim remains narrow and still appears non-colliding in this pass.

## Files changed

- `paper/references.bib`
  - Added `vincent2026coughsense`.
- `paper_icassp2027/main.tex`
  - Added `vincent2026coughsense` to the symptom-assisted and multimodal respiratory modeling citation group.

## Compile check

Commands:

```cmd
pdflatex -interaction=nonstopmode main.tex
bibtex main
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
```

Status:

- `bibtex` succeeded, with only the known MiKTeX user-log permission message.
- Final `pdflatex` succeeded.
- Output: `main.pdf (5 pages, 233245 bytes)`.
- No `Undefined`, `LaTeX Warning`, `Package Warning`, `Error`, or `Overfull` matches in `main.log`.
- Remaining messages are underfull layout notices only.

PDF text check:

- `main_pdftotext.txt` contains:
  - "semantic-constrained router"
  - "a safety prior for transfer routing under cough dataset shift"
  - "[12] Nikhil Vincent, CoughSense"
- `main.bbl` contains `\bibitem{vincent2026coughsense}`.

Page character distribution after adding the citation:

| page | extracted chars |
| ---: | ---: |
| 1 | 4620 |
| 2 | 5232 |
| 3 | 5698 |
| 4 | 5459 |
| 5 | 3165 |

The paper remains 5 pages.

## Next best actions

1. Low-cost paper action:
   - Keep this citation and claim boundary.
   - Do not expand the main text unless a direct collision appears.
2. Evidence action:
   - Third-target validation remains the strongest way to move from credible candidate to acceptance-ready claim.
   - UKCOVID is still the most natural target but requires large audio access/download approval.
3. If large assets remain disallowed:
   - Add a supplementary novelty matrix with CoughSense, SympCoughNet, device-invariant multimodal respiratory modeling, GLoRIA, semantic descriptors, and negative-transfer surveys.

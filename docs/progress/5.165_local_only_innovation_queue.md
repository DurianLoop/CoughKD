# 5.165 Local-Only Innovation Queue

Date: 2026-06-16

User constraint: use the datasets already present in the workspace. Do not
start new dataset downloads, training runs, or inference sweeps without explicit
approval.

## Literature refresh

The latest local-only collision pass keeps the current claim narrow.

Closest current neighbors:

- CoughSense, arXiv:2606.02998: multi-dataset cough classification with symptom
  conditioning, OPERA/Whisper-style representations, and domain-adaptation
  components. This is close to cough-domain adaptation, but it is not a
  deployment router that chooses between transfer strategies by target metadata
  field semantics.
- GLoRIA, arXiv:2603.02464: metadata-gated low-rank adaptation for dialectal
  ASR. This is close to metadata-gated audio adaptation, but it adapts model
  parameters rather than routing between slice-gated transfer and calibration
  stacking.
- BTS, arXiv:2406.06786: metadata-aided respiratory sound classification using
  metadata-derived text. This is close to metadata-as-context respiratory
  modeling, but it is not a safety prior over metadata field types for transfer
  selection.

Readout: no direct collision was found for the current field-semantics transfer
router unit. This is still a scoped audit, not absolute proof that nobody has
done it.

## New artifact

Added `scripts/summarize_semantic_router_local_only_innovation_queue.py`.

Generated:

- `runs/semantic_router_local_only_innovation_queue/SEMANTIC_ROUTER_LOCAL_ONLY_INNOVATION_QUEUE.md`
- `runs/semantic_router_local_only_innovation_queue/semantic_router_local_only_innovation_queue.json`

Current verdict: `LOCAL_ONLY_QUEUE_ACTIVE`.

Queue:

1. `semantic_router_official_calibration_sufficiency`
   - status: `awaiting_user_approval`
   - data: existing TosCOVID calib10 subset only
   - compute: inference only, no training, no download
   - gate: stop below 1 pp; expand stable subset above 1 pp; consider full-router
     only above 3 pp
   - claim role: reviewer-facing rigor upgrade, not a third-target replacement
2. `semantic_router_current_two_target_claim`
   - status: `credible_not_final_ready`
   - claim role: current narrow semantic-router claim
3. `local_micro_stress_only`
   - status: `auxiliary_only`
   - claim role: supplement stress evidence only
4. `do_not_mark_complete`
   - status: `guardrail`
   - claim role: process guard

## Gate integration

Updated:

- `scripts/audit_semantic_router_submission_readiness.py`
  - adds `local_only_innovation_queue`
  - passes when the queue is active, has candidates, and `large_external_data`
    is false
- `scripts/build_semantic_router_claim_dossier.py`
  - adds `Local-only innovation queue` as a process requirement
  - keeps it separate from scientific evidence
- `scripts/audit_semantic_router_goal_completion.py`
  - adds `local_only_innovation_queue`
  - keeps the full goal `NOT_COMPLETE` until the hard claim blockers clear

## Current gate state

After refresh:

- Submission readiness: `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- Claim dossier: `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- Goal completion: `NOT_COMPLETE`
- Next action: `AWAIT_CALIB10_STABLE_SMOKE`
- Large external data: `False`
- Requires user approval: `True`

No inference, training, or download was run in this step.

## Next action

If approved, run:

```cmd
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

This runs 9 existing-checkpoint inference jobs over 500 existing TosCOVID train
calibration rows. It does not train models and does not download data.

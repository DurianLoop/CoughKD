# 5.166 No-Inference Alternative Frontier

Date: 2026-06-16

Constraint: continue the ICASSP 2027 innovation loop using existing local
datasets and existing prediction artifacts. Do not run unapproved inference,
training, or downloads.

## Literature refresh

Recent adjacent areas remain crowded:

- test-time adaptation for audio/domain shift;
- model ensembles under target shift;
- calibration and posterior correction;
- metadata-aided respiratory classification.

This makes a generic no-retraining ensemble, calibration transform, or
confidence/entropy selection rule a weak novelty route unless it has a
cough-specific mechanism and clears both large local targets.

## New artifact

Added `scripts/summarize_semantic_router_no_inference_frontier.py`.

Generated:

- `runs/semantic_router_no_inference_frontier/SEMANTIC_ROUTER_NO_INFERENCE_FRONTIER.md`
- `runs/semantic_router_no_inference_frontier/semantic_router_no_inference_frontier.json`

Current verdict: `NO_INFERENCE_FRONTIER_REVIEWED`.

## Readout from existing artifacts

The script reads only existing audit outputs:

- `runs/prediction_ensemble_frontier/prediction_ensemble_frontier_summary.json`
- `runs/target_calibrated_stacking/target_calibrated_stacking_summary.json`
- `runs/metadata_slice_oracle/metadata_slice_oracle_summary.json`
- `runs/posterior_prior_correction/posterior_prior_correction_summary.json`
- `runs/transfer_gap_diagnostics/transfer_gap_diagnostics.json`

Main conclusions:

1. Plain prediction ensembles do not clear the target claim on both large
   targets.
   - COUGHVID best ensemble: +1.05 pp
   - TosCOVID best ensemble: +0.73 pp
   - Virufy/Virufyseg gains are auxiliary only because the independent sample
     size is too small for a robust main claim.
2. Global target-calibrated stacking is not a replacement claim.
   - COUGHVID best mean delta: +0.49 pp
   - TosCOVID best mean delta: +3.57 pp
   - This asymmetry supports using stacking as a baseline/component, not as the
     main ICASSP innovation.
3. Metadata slice oracle remains mechanism support only.
   - It clears a large-target oracle gate, but it is label-revealed.
   - It should support the semantic field prior, not become a deployable claim.
4. Posterior/prior correction is too small on large targets.
   - Maximum large-target delta: +0.48 pp
5. Transfer-gap diagnostics are useful but not yet a method.
   - The current diagnostics are label-free, but a plain confidence/entropy rule
     would collide with broad uncertainty KD/TTA literature.

## Gate integration

Updated:

- `scripts/summarize_semantic_router_local_only_innovation_queue.py`
  - adds `prediction_only_alternative_frontier`
- `scripts/audit_semantic_router_submission_readiness.py`
  - adds `no_inference_frontier`
- `scripts/build_semantic_router_claim_dossier.py`
  - adds `No-inference alternative frontier`
- `scripts/audit_semantic_router_goal_completion.py`
  - adds `no_inference_alternative_frontier`

After refresh:

- Submission readiness: `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- Claim dossier: `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- Goal completion: `NOT_COMPLETE`
- Next action: `AWAIT_CALIB10_STABLE_SMOKE`

No inference, training, or download was run.

## Decision

Do not pivot to a prediction-only replacement claim. These routes are now
recorded as negative frontier / mechanism-support boundary evidence. The current
best local-only next step remains the approved-by-user-only TosCOVID calib10
stable smoke probe.

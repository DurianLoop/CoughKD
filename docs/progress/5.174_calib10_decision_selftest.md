# 5.174 Calib10 Decision Self-Test

Date: 2026-06-16

Constraint: use existing local datasets only. No inference, training, or data
download was run.

## Why this was added

The calib10 stable probe has a preregistered branch rule:

- below 1 pp: stop the official-split enhancement route;
- 1 pp to below 3 pp: ask before the next stable subset;
- at least 3 pp: ask before full-router escalation.

This rule now has a logic-only self-test so a boundary-condition bug cannot
silently route a failed experiment into extra compute or turn a marginal result
into a stronger claim.

## New artifact

Added:

- `scripts/audit_toscovid_calib10_decision_selftest.py`
- `runs/toscovid_calib10_decision_selftest/TOSCOVID_CALIB10_DECISION_SELFTEST.md`
- `runs/toscovid_calib10_decision_selftest/toscovid_calib10_decision_selftest.json`

The self-test covers:

- negative delta;
- just below 1 pp;
- exactly 1 pp;
- between 1 pp and 3 pp;
- just below 3 pp;
- exactly 3 pp;
- above 3 pp.

Current verdict: `PASS`.

## Integration

Updated:

- `scripts/run_toscovid_official_calibration_probe_windows.cmd`
- `scripts/audit_semantic_router_submission_readiness.py`
- `scripts/audit_semantic_router_goal_completion.py`

After an approved calib10 non-dry-run, the driver will refresh the decision
self-test before refreshing the semantic-router gates.

## Verification

Passed/refreshed:

- `scripts/audit_toscovid_calib10_decision_selftest.py`: `PASS`
- `scripts/audit_semantic_router_submission_readiness.py`:
  `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- `scripts/build_semantic_router_claim_dossier.py`:
  `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- `scripts/audit_semantic_router_goal_completion.py`: `NOT_COMPLETE`

Readiness now includes:

- `toscovid_calib10_decision_selftest`: `PASS`
- `failure_handoff`: `PASS`

The only current submission-readiness hard blocker remains:

- `third_target_ukcovid`: `NOT_READY_AUDIO_AND_PREDICTIONS_MISSING`

The active compute step remains approval-bound:

```cmd
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

# 5.122 UKCOVID third-target preflight

## Purpose

Continue the ICASSP 2027 objective by addressing the largest remaining empirical gap:

> Does the semantic-router claim generalize to a third large external target?

No large download, no training, and no external evaluation were run in this round.

## Readiness check

Command:

```cmd
D:\conda\envs\CoughKD\python.exe -B scripts\check_ukcovid_audio_ready.py
```

Result:

```json
{
  "verdict": "NOT_READY_AUDIO_MISSING",
  "archive_complete": false,
  "manifest_rows": 22242,
  "manifest_subjects": 11121,
  "unique_audio_filenames": 22242,
  "unique_audio_found": 0,
  "unique_audio_missing": 22242
}
```

Interpretation:

- UKCOVID metadata and test manifest are ready.
- Audio archive files are not local:
  - archive files complete: `0/25`;
  - unique manifest audio found: `0/22242`.
- The blocker is external-state approval/download/extraction, not manifest readiness.

## Metadata suitability

Existing metadata gate readiness confirms UKCOVID is a strong third-target candidate:

- rows: `22242`;
- subjects: `11121`;
- labels:
  - healthy: `14602`;
  - covid_positive: `7640`;
- ready metadata features: `20`;
- strong symptom fields include:
  - `symptom_cough_any`;
  - `symptom_fatigue`;
  - `symptom_headache`;
  - `symptom_onset`;
  - `symptom_none`;
- demographic controls include:
  - `age`;
  - `sex`.

This supports a direct third-target test of the semantic-router rule:

- symptom/acquisition metadata may support slice-gated transfer;
- demographic-only metadata should not be treated as equivalent slice-gating variables.

## Code prepared

Small registry-level changes were made so UKCOVID can be used once audio predictions exist:

- `scripts/audit_prediction_ensemble_frontier.py`
  - recognizes runs named `external_ukcovid_open_<method>_seed<seed>`.
- `scripts/audit_metadata_slice_oracle.py`
  - adds target `UKCOVID`;
  - derives UKCOVID symptom flags from the manifest `symptoms` string.
- `scripts/audit_target_calibrated_transfer_controller.py`
  - adds UKCOVID slice config:
    - `slice_column = symptom_cough_any`;
    - `min_train_slice = 200`;
    - `switch_margin = 0.02`.
- `scripts/audit_semantic_constrained_transfer_router.py`
  - adds UKCOVID symptom fields to semantic slice columns.

Validation:

- AST parse check passed for the four changed scripts.
- UKCOVID semantic-router preflight completed without crashing:

```cmd
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_constrained_transfer_router.py ^
  --targets UKCOVID ^
  --n-repeats 1 ^
  --out runs\semantic_router_ukcovid_preflight_noaudio
```

Expected output:

- no rows, because UKCOVID prediction CSVs do not exist before audio evaluation;
- semantic slice columns include UKCOVID symptom fields.

Note:

- `py_compile` was attempted first but failed because Windows denied writing into `scripts\__pycache__`.
- A no-pyc `ast.parse` syntax check was used instead and passed.

## Execution pack

Added:

- `runs/semantic_router_third_target_execution_pack/UKCOVID_SEMANTIC_ROUTER_EXECUTION_PACK.md`

The pack contains:

- current readiness status;
- approval needed;
- download command;
- post-download readiness command;
- manifest rebuild command;
- external onboarding command;
- semantic-router 1000-repeat command;
- UKCOVID negative-control commands;
- success and rejection gates.

## Dataset tracker update

Updated:

- `docs/data_access/dataset_access_tracker.csv`

UKCOVID status remains:

- `metadata_manifest_ready`

But next action now records that semantic-router scripts are pre-registered and the remaining action is audio download/extraction.

## Claim status

This round improves execution readiness but does not complete the objective.

Current state:

- current two-target claim: credible candidate;
- third-target readiness: prepared, but audio missing;
- final ICASSP objective: still incomplete.

## Next action

Ask for approval before any heavy action:

- approve UKCOVID audio download/extraction if the priority is the strongest empirical ICASSP claim;
- otherwise keep the current two-target paper with explicit third-target limitation and supplement bundle.

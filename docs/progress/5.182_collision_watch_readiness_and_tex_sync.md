# 5.182 Collision-watch readiness and TeX sync

This note records a synchronization pass after adding the latest collision-watch
gate.

## Problem

The latest collision watch had been connected to the claim dossier and
goal-completion audit, but two reviewer-facing paths were still weaker:

- the LaTeX novelty matrix did not yet include the TB clinical-metadata cough
  baseline row;
- submission readiness did not list the collision-watch artifact as a required
  checklist item.

## Changes

Updated:

- `runs/semantic_router_novelty_matrix/semantic_router_novelty_matrix.tex`
- `scripts/audit_semantic_router_submission_readiness.py`
- `scripts/audit_semantic_router_claim_consistency.py`

New LaTeX novelty row:

- `Clinical-metadata cough baselines`
- representative source: `TB cough audio + clinical metadata baseline (2026)`
- boundary: not first cough-audio clinical-metadata fusion, protocol
  standardization, or clinical utility.

Readiness now includes:

- `collision_watch | PASS`

Claim consistency now includes:

- `novelty_matrix_tex_boundary | PASS`

The source-ledger consistency check also requires:

- `https://arxiv.org/abs/2603.15688`
- `https://arxiv.org/abs/2601.07969`

## Verification

Commands run:

```powershell
D:\conda\envs\CoughKD\python.exe -B scripts\summarize_semantic_router_collision_watch.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_claim_consistency.py
D:\conda\envs\CoughKD\python.exe -B scripts\build_semantic_router_claim_dossier.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_goal_completion.py
```

Results:

- collision watch: `NO_DIRECT_COLLISION_FOUND_IN_LATEST_WATCH`
- claim consistency: `PASS`
- claim dossier: `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- submission readiness: `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- goal completion: `NOT_COMPLETE`

Important line-level evidence:

- readiness contains `collision_watch | PASS`;
- claim consistency contains `novelty_matrix_tex_boundary | PASS`;
- LaTeX novelty matrix contains `Clinical-metadata cough baselines`.

The next experiment remains approval-bound:

```powershell
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

# 5.62 ICASSP template migration status

## 当前状态

已新增 ICASSP 草稿目录：

```text
paper_icassp2027\
```

其中包含：

- `main.tex`
- `spconf.sty`
- `IEEEbib.bst`
- `compile_windows.cmd`

由于 ICASSP 2027 独立 author kit 尚未完全固定，本轮先使用 ICASSP 2026 官方 paper kit 中的 `spconf.sty` 与 `IEEEbib.bst` 作为临时模板。后续 ICASSP 2027 author kit 发布后，需要替换模板文件并重新检查格式。

## 编译结果

已完成：

```cmd
cd /d D:\CoughKD\AAAI\paper_icassp2027
compile_windows.cmd
```

当前结果：

- `paper_icassp2027\main.pdf` 可成功生成；
- BibTeX 引用已解析；
- 无 undefined citation；
- 无 LaTeX error；
- 无 Overfull hbox；
- 当前 PDF 为 3 页。

## 当前稿件形态

本轮已将 `paper_icassp2027\main.tex` 从长版 section input 改成 ICASSP 专用自包含短稿。当前主文保留：

- Abstract + keywords；
- Introduction；
- CoughKD-ShiftAudit 方法/审计协议；
- Guarded selection；
- Experimental setup；
- Main results；
- Discussion；
- Conclusion；
- COUGHVID audit table；
- target/unit stress table；
- deployment efficiency table；
- metric disagreement figure；
- protocol figure。

当前 3 页版本已经满足页数上限，但作为正式投稿还略偏短。后续可以利用剩余空间补充：

- 更清晰的数据集表；
- loss/guard 公式；
- Tos/COUGHVID label and split caveat；
- reviewer-risk paragraph；
- appendix/supplement 指向。

## 压缩/扩展策略

保留主文：

1. 一张主结果表：COUGHVID external audit；
2. 一张 target/unit stress table：COUGHVID + Tos + Virufy stress；
3. 一张 deployment efficiency table；
4. 一张方法/协议图；
5. 一张小的 metric-disagreement figure。

压缩或移到 appendix：

- 过长 related work；
- 完整 candidate A/B/C 描述；
- 细粒度 slice/bootstrap 图；
- 过多 reviewer caveat；
- UK/Cambridge 数据访问过程；
- Virufy subject aggregation细节。

## 当前 claim 不变

当前投稿主线保持为：

> CoughKD-ShiftAudit is a bounded deployment audit showing that ultra-light cough audio distillation under dataset shift is method-sensitive and target-sensitive. Across COUGHVID and Tos COVID-19, common KD/TCD variants provide weak, unstable, or negative external gains; Virufy stress targets further show evaluation-unit sensitivity. This supports an analysis/audit contribution, not a new KD method-superiority or clinical diagnosis claim.

## 2026-06-03 追加：ICASSP 短稿正式化增强

本轮在 `paper_icassp2027\main.tex` 中补强了正式投稿稿件需要的三个部分：

- 新增数据集/协议表 `Table~\ref{tab:datasets}`：列出 Coswara、COUGHVID、Tos COVID-19、Virufy、Virufy-seg 的 clips、subjects、COVID-positive clips 和论文角色；
- 新增 audit loss 公式：把 CE、KD、TCD、probe penalty 统一写成审计模板，明确这些项是被审计的常规鲁棒 KD 控制，而不是单独宣称原创；
- 新增 target-unlabeled guard 公式：把 confidence、entropy、predicted-label balance、source-relative shift 写成 guard score，并强调 guard 只通过外部标签后的结果来判定是否可靠；
- 新增 threats-to-validity 段落：明确没有控制症状、人口统计、设备和招募渠道，因此结果只能解释为 public-dataset shift 下的蒸馏可靠性证据，不能解释为疾病机制或临床有效性。

重新运行：

```cmd
cd /d D:\CoughKD\AAAI\paper_icassp2027
compile_windows.cmd
```

当前编译结果：

- `paper_icassp2027\main.pdf` 成功生成；
- PDF 当前为 4 页；
- 最终 `main.log` 无 undefined citation/reference；
- 最终 `main.log` 无 LaTeX Error；
- 最终 `main.log` 无 Overfull hbox；
- 仅有少量 Underfull hbox/vbox，属于双栏短稿常见排版警告。

项目级审计结果：

```cmd
D:\conda\envs\CoughKD\python.exe -B scripts\audit_claim_boundary.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_submission_readiness.py
```

- claim-boundary: `PASS`
- submission readiness: `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING`

阶段判断：短稿已经从 smoke version 进入可继续精修的 4-page ICASSP 形态。下一步优先做语言压缩、图表清晰度和 reviewer-risk polish，而不是继续扩展 teacher × student × KD 组合。

## 2026-06-03 追加：主结果表双栏化

视觉 QA 发现原始单栏 COUGHVID audit table 和 target/unit stress table 字号过小。已修改生成脚本：

- `scripts\build_shift_audit_paper_tables.py`

现在两张核心结果表使用：

- `table*`
- `\textwidth`

并重新生成：

- `runs\kd_failure_analysis\paper_ready_tables\paper_ready_tables.tex`

重新编译 `paper_icassp2027\main.pdf` 后：

- PDF 仍为 4 页；
- 主结果表和 target/unit stress 表在第 3 页顶部以双栏宽表显示；
- 最终 log 无 undefined citation/reference；
- 无 LaTeX Error；
- 无 Overfull hbox；
- claim-boundary: `PASS`；
- submission readiness: `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING`。

该改动提升了正式投稿版本的可读性，并且已写入生成脚本，后续重新生成表格不会退回单栏小表。

## 自动审计

本轮重新运行：

```cmd
D:\conda\envs\CoughKD\python.exe -B scripts\audit_claim_boundary.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_submission_readiness.py
```

结果：

- claim-boundary: `PASS`
- submission readiness: `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING`

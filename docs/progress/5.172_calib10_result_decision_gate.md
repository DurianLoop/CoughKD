# 5.172 Calib10 Result Decision Gate

Date: 2026-06-16

Constraint: use existing local datasets only. No inference, training, or data
download was run.

## Why this was added

The active branch is still `AWAIT_CALIB10_STABLE_SMOKE`, but the project needed
a machine-readable rule for what happens after the approved calib10 stable probe
eventually produces predictions.

This prevents post-hoc interpretation drift:

- below 1 pp official-test delta: stop the TosCOVID official-split enhancement
  route and return to the local-only literature/innovation loop;
- 1 to 3 pp: ask before running the next larger stable calibration subset;
- at least 3 pp: ask before spending on full-router escalation.

## New artifact

Added:

- `scripts/audit_toscovid_calib10_result_decision.py`
- `runs/toscovid_calib10_result_decision/TOSCOVID_CALIB10_RESULT_DECISION.md`
- `runs/toscovid_calib10_result_decision/toscovid_calib10_result_decision.json`

Current result:

- Verdict: `AWAITING_APPROVED_INFERENCE`
- State: `AWAIT_CALIB10_STABLE_SMOKE`
- Missing prediction files: `9`
- Approval required: `True`
- Large external data: `False`

This is not a failure result. It means the approved inference has not been run.

## Integration

Updated:

- `scripts/run_toscovid_official_calibration_probe_windows.cmd`
- `scripts/summarize_semantic_router_branch_controller.py`
- `scripts/summarize_semantic_router_local_only_innovation_queue.py`
- `scripts/audit_semantic_router_submission_readiness.py`

After an approved non-dry-run probe, the driver now refreshes:

1. TosCOVID calibration budget;
2. calib10 result decision;
3. local-only queue;
4. branch controller;
5. submission readiness;
6. claim dossier;
7. goal completion.

## Verification

Passed/refreshed:

- `scripts/audit_toscovid_calib10_result_decision.py`:
  `AWAITING_APPROVED_INFERENCE`
- `scripts/summarize_semantic_router_local_only_innovation_queue.py`:
  `LOCAL_ONLY_QUEUE_ACTIVE`
- `scripts/summarize_semantic_router_branch_controller.py`:
  `BRANCH_CONTROLLER_ACTIVE`
- `scripts/audit_semantic_router_submission_readiness.py`:
  `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- `scripts/build_semantic_router_claim_dossier.py`:
  `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- `scripts/audit_semantic_router_goal_completion.py`: `NOT_COMPLETE`

The only submission-readiness hard blocker remains:

- `third_target_ukcovid`: `NOT_READY_AUDIO_AND_PREDICTIONS_MISSING`

The next compute step remains approval-bound:

```cmd
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

# 5.173 Failure-Handoff Preregistration

Date: 2026-06-16

Constraint: use existing local datasets only. No inference, training, or data
download was run.

## Why this was added

The active experiment branch remains approval-bound:

```cmd
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

If that calib10 stable probe later fails below the 1 pp continuation threshold,
the project should not drift into post-hoc threshold tuning or revive already
weak candidates. This note preregisters the local-only handoff before seeing any
calib10 result.

## New artifact

Added:

- `scripts/summarize_semantic_router_failure_handoff.py`
- `runs/semantic_router_failure_handoff/SEMANTIC_ROUTER_FAILURE_HANDOFF.md`
- `runs/semantic_router_failure_handoff/semantic_router_failure_handoff.json`

Current status:

- Verdict: `FAILURE_HANDOFF_PRE_REGISTERED`
- State: `WAIT_FOR_CALIB10_RESULT`
- Active: `False`

This means the failure branch is documented but dormant. It should activate only
if the calib10 result-decision gate reports
`STOP_OFFICIAL_SPLIT_ENHANCEMENT_ROUTE`.

## Handoff queue

The handoff queue records these rules:

- keep `official_calibration_sufficiency` as the only active low-cost compute
  branch until calib10 is approved and evaluated;
- do not promote prediction-only alternatives already rejected by the
  no-inference frontier;
- do not train target-coverage-aware artifact alignment yet, because the
  existing artifact route is COUGHVID-positive but TosCOVID-negative;
- do not treat CE/plain target stacking as a new innovation;
- do not pivot to broad audio-language/metadata foundation claims, because
  RespiraMFM, AcuLa, BTS, and related work crowd that route;
- guarded failure-slice auditing remains a possible paper-framing fallback, but
  it does not satisfy the current positive 1-3 pp innovation objective by
  itself.

## Literature risk update

Updated novelty anchors:

- RespiraMFM, arXiv:2606.09966
- AcuLa, arXiv:2512.04847
- SLM-TTA, arXiv:2512.24739
- CoughSense, arXiv:2606.02998

RespiraMFM was also added to the novelty source ledger as the current arXiv
anchor for multimodal respiratory audio-text alignment.

## Verification

Passed/refreshed:

- `scripts/summarize_semantic_router_failure_handoff.py`:
  `FAILURE_HANDOFF_PRE_REGISTERED`
- `scripts/audit_semantic_router_claim_consistency.py`: `PASS`
- `scripts/build_semantic_router_claim_dossier.py`:
  `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- `scripts/audit_semantic_router_goal_completion.py`: `NOT_COMPLETE`
- `scripts/audit_semantic_router_submission_readiness.py`:
  `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`

The only submission-readiness hard blocker remains:

- `third_target_ukcovid`: `NOT_READY_AUDIO_AND_PREDICTIONS_MISSING`

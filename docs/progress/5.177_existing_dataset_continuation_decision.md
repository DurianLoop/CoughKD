# 5.177 Existing-dataset continuation decision

This note records a low-cost continuation step for the ICASSP 2027 innovation
loop after the user clarified that the current project should use the datasets
already present in the workspace.

## User constraint

Use existing local datasets. Do not download new external datasets, and do not
run training or inference unless explicitly approved.

## Literature sweep readout

Targeted query families checked in this pass:

- `2026 cough audio metadata transfer routing symptom demographic calibration stacking`
- `metadata-gated audio adaptation transfer learning respiratory cough 2026`
- `calibration stacking cough metadata respiratory audio`
- `target metadata transfer routing cough`
- `metadata field slice gating transfer learning audio`

Closest current neighbors:

- CoughSense, arXiv:2606.02998:
  - close to symptom-conditioned and domain-adaptive cough modeling;
  - not a field-semantics transfer router with all/no/inverted semantic controls.
- GLoRIA, arXiv:2603.02464:
  - close to metadata-gated audio adaptation;
  - not a router that chooses between slice-gated transfer and calibration
    stacking by metadata field safety.
- PulmoVec, arXiv:2603.15688:
  - close to demographic metadata stacking;
  - supports the boundary that demographic metadata stacking is crowded and
    should not be claimed as novel.
- AcuLa / BTS / RespiraMFM:
  - close to metadata-to-text and audio-language respiratory modeling;
  - not a replacement for the narrower transfer-strategy routing claim.

No direct collision was found for the current narrow claim:

> target metadata fields are not interchangeable transfer variables; symptom or
> acquisition fields may support slice-gated transfer, while demographic-only
> fields route to calibration stacking.

This remains a scoped collision check, not absolute proof of novelty.

## New artifact

Added:

- `scripts/summarize_existing_dataset_continuation_decision.py`
- `runs/semantic_router_existing_dataset_decision/EXISTING_DATASET_CONTINUATION_DECISION.md`
- `runs/semantic_router_existing_dataset_decision/semantic_router_existing_dataset_decision.json`

The new gate is deliberately non-experimental. It reads current artifacts and
records the active local-only decision:

- verdict: `EXISTING_DATASET_DECISION_ACTIVE`
- decision: `ASK_BEFORE_CALIB10_INFERENCE`
- state: `AWAIT_CALIB10_STABLE_SMOKE`
- approval required: `True`
- large external data: `False`

It also closes the following routes under the current user constraint:

- new external dataset download;
- training or inference without approval;
- prediction-only replacement claims;
- local third-target claims from current Virufy/Virufyseg artifacts.

## Ledger fix

The existing novelty matrix already mentioned PulmoVec, but the source ledger
did not contain a standalone PulmoVec row. Added a PulmoVec entry to:

- `runs/semantic_router_novelty_matrix/SEMANTIC_ROUTER_SOURCE_LEDGER.md`

After this fix, the continuation-decision gate reports all expected literature
ledger hits as present.

## Verification

Commands run:

```powershell
D:\conda\envs\CoughKD\python.exe -B scripts\summarize_existing_dataset_continuation_decision.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_claim_consistency.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_goal_completion.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
```

Results:

- existing-dataset decision: `EXISTING_DATASET_DECISION_ACTIVE`
- claim consistency: `PASS`
- goal completion: `NOT_COMPLETE`
- submission readiness: `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`

The active next step remains the approval-bound existing-data smoke:

```powershell
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

Do not run it unless the user explicitly approves inference.

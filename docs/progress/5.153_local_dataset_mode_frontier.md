# 5.153 Local Dataset Mode Frontier

Date: 2026-06-15

User constraint: use the datasets already present in the workspace; do not keep treating a large UKCOVID download as the active route unless explicitly approved later.

## What changed

- Refreshed `runs/local_external_target_readiness/LOCAL_EXTERNAL_TARGET_READINESS.md` after fixing the audio-root check to include `D:\CoughKD`.
- Confirmed local audio is present for:
  - `coughvid_external.csv`: 7310 / 7310 files
  - `toscovid2021_test_external.csv`: 991 / 991 files
  - `virufy_external.csv`: 16 / 16 files
  - `virufy_segmented_external.csv`: 121 / 121 files
  - `coswara_cough.csv`: 5491 / 5491 files
- Added `scripts/bootstrap_subject_ensemble_delta.py` for subject-level, seed-ensemble paired bootstrap checks.
- Added `scripts/summarize_semantic_router_local_dataset_frontier.py` and generated `runs/semantic_router_local_dataset_frontier/SEMANTIC_ROUTER_LOCAL_DATASET_FRONTIER.md`.
- Updated `scripts/summarize_semantic_router_next_action.py` so the active dashboard no longer defaults to waiting for UKCOVID approval when local-dataset mode is in force.
- Integrated the local micro-stress evidence into `runs/semantic_router_supplement/main.tex` and recompiled the supplement PDF.
- Updated `scripts/audit_semantic_router_submission_readiness.py`, `scripts/build_semantic_router_claim_dossier.py`, and `scripts/audit_semantic_router_goal_completion.py` so they track the local-only frontier without weakening the third-target completion gate.

## Evidence

Virufyseg subject-level seed-ensemble stress test:

| Candidate | Subjects | Delta vs source-only | 95% CI | P(delta <= 0) | P(delta < 3 pt) |
|---|---:|---:|---:|---:|---:|
| candidate_c_seed_ensemble | 16 | +0.095238 | -0.027273 .. +0.238095 | 0.096048 | 0.168584 |
| kd_seed_ensemble | 16 | +0.071429 | -0.045455 .. +0.216667 | 0.146073 | 0.276138 |

## Decision

Verdict: `NO_LOCAL_THIRD_TARGET_READY`.

Local data can strengthen the supplement with Virufy/Virufyseg as auxiliary micro-stress evidence, but it should not be used to claim a robust third external target because the effective independent sample size is only 16 subjects. Coswara is audio-ready but is the source domain, so it should not be counted as an external transfer target.

Current next-action state: `LOCAL_DATASET_MODE_NO_THIRD_TARGET`.

Current claim dossier: `CREDIBLE_CANDIDATE_NOT_FINAL_READY`.

Current goal-completion audit: `NOT_COMPLETE`.

## Next low-cost move

Keep COUGHVID and TosCOVID as the two main semantic-router targets, include Virufyseg only as a clearly labeled auxiliary stress test, and continue local-only novelty search before requesting any large download or heavy training run.

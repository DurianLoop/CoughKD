# 5.170 ICASSP Page-Rule Gate

Date: 2026-06-16

Constraint: use existing local artifacts only. No inference, training, or data
download was run.

## What changed

Added `scripts/audit_icassp_page_rule.py` and connected its output to the
existing quality/readiness gates.

The new audit checks:

- the local ICASSP 2027 rule record exists;
- `paper_icassp2027/main_pdftotext.txt` has at most 5 extracted pages;
- if page 5 exists, it is nonempty and has no main-text section, table, figure,
  equation, or core-claim pattern;
- the audit only reads existing PDF text and does not compile, train, infer, or
  download data.

## Current result

`runs/icassp_page_rule/ICASSP_PAGE_RULE.md` reports:

- Verdict: `PAGE_RULE_PASS`
- Extracted PDF pages: `5`
- Page 5 forbidden hits: `0`

This means the current 5-page draft passes the recorded page-shape rule: the
fifth page is treated as references-only by the automated scan.

## Gate integration

Updated:

- `scripts/audit_icassp_draft_quality.py`
- `scripts/audit_semantic_router_submission_readiness.py`
- `scripts/audit_semantic_router_goal_completion.py`
- `scripts/build_semantic_router_claim_dossier.py`

The page-rule audit is now a required local artifact for draft/readiness
checking. Passing it does not mark the project complete, because the final
ICASSP 2027 author kit/template still needs to be verified before submission.

## Verification

Passed/refreshed:

- `scripts/audit_icassp_page_rule.py`: `PAGE_RULE_PASS`
- `scripts/audit_icassp_draft_quality.py`: `PASS`
- `scripts/build_semantic_router_claim_dossier.py`:
  `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- `scripts/audit_semantic_router_goal_completion.py`: `NOT_COMPLETE`
- `scripts/audit_semantic_router_submission_readiness.py`:
  `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`

The only current submission-readiness blocker remains:

- `third_target_ukcovid`: `NOT_READY_AUDIO_AND_PREDICTIONS_MISSING`

The final ICASSP 2027 author kit/template remains a warning.

## Next low-cost branch

The active branch remains `AWAIT_CALIB10_STABLE_SMOKE`.

Do not run the TosCOVID calib10 stable probe until the user explicitly approves:

```cmd
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

# 5.113 Targeted collision search and multimodal metadata anchor

## Purpose

Continue the novelty audit for the semantic-constrained transfer router, focusing on direct collisions:

- metadata-conditioned model selection;
- metadata type safety;
- demographic-vs-symptom routing;
- safe transfer routing in medical audio.

This note also records one newly added adjacent citation.

## Search queries used

Targeted web queries included:

- `"metadata-conditioned" "model selection" "domain adaptation"`
- `"metadata" "model selection" "safe transfer"`
- `"metadata type" "domain adaptation" "negative transfer"`
- `"demographic" "symptom" "routing" "domain adaptation"`
- `"metadata-conditioned" "ensemble" "medical" "model selection"`
- `"clinical metadata" "model selection" "domain adaptation"`
- `"safe transfer" "medical audio" "domain adaptation"`
- `"metadata semantics" "transfer routing"`
- `"semantic-constrained" "routing" "domain adaptation"`
- `"symptom" "demographic" "slice" "cough" "transfer"`

## Main adjacent work found

Newly surfaced adjacent paper:

- Yang et al., "A Device-Invariant Multi-Modal Learning Framework for Respiratory Disease Classification," npj Digital Medicine 2026.
- URL: https://www.nature.com/articles/s41746-026-02445-4

Why it matters:

- It uses cough/respiratory audio together with demographics, symptom descriptions, disease descriptions, and device metadata.
- It is highly relevant to any claim involving metadata and respiratory audio.
- It further confirms that the semantic-router paper must not claim "first respiratory metadata model" or "first multimodal respiratory model."

Why it is not a direct collision:

- It proposes a device-invariant multimodal learning framework, not a transfer-strategy router.
- It does not appear to classify target metadata fields into safe vs unsafe routing variables.
- It does not appear to compare symptom/acquisition slice gates against demographic-only slice gates.
- It does not appear to use inverted semantic-rule negative controls where flipping metadata field semantics collapses transfer gains.

## Paper updates

Updated:

- `paper/references.bib`
- `paper_icassp2027/main.tex`

Added BibTeX key:

- `yang2026deviceinvariant`

Main-text change:

- Replaced "symptom-assisted cough modeling" with "symptom-assisted and multimodal respiratory modeling" and cited both:
  - `lin2025sympcoughnet`
  - `yang2026deviceinvariant`

This makes the novelty boundary stronger:

- metadata and multimodal respiratory modeling are explicitly prior work;
- the claim remains metadata-semantics-constrained transfer routing.

## Build check

Commands run:

```cmd
pdflatex -interaction=nonstopmode main.tex
bibtex main
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
```

Status:

- BibTeX completed successfully, with only a MiKTeX user-log permission message.
- `main.bbl` contains the new npj Digital Medicine citation.
- Final `pdflatex` log scan found no `Undefined`, `Warning`, `Error`, or `Overfull` lines.
- `main.pdf` output: 5 pages, 231,981 bytes.
- `pdftotext` updated `main_pdftotext.txt`, with only the same MiKTeX user-log permission message.

Page distribution:

| page | extracted chars | notes |
| ---: | ---: | --- |
| 1 | 4502 | abstract/introduction |
| 2 | 5068 | semantic-router method and setup |
| 3 | 5436 | results and semantic tables |
| 4 | 5033 | discussion/conclusion plus references start |
| 5 | 2513 | references |

## Collision status

No direct collision found in this targeted pass.

The closest adjacent families remain:

1. Symptom-assisted cough detection.
2. Multimodal respiratory metadata modeling.
3. Negative-transfer mitigation / safe transfer.
4. Semantic descriptors for domain adaptation.
5. Metadata-conditioned or target-conditioned model selection.

Current interpretation:

- The semantic router is not novel as metadata use, symptom use, multimodal learning, domain adaptation, or safe transfer.
- The potentially novel unit remains the safety prior over metadata field type for routing between transfer mechanisms.

Current narrow claim:

> In COVID cough transfer, target metadata fields should not be treated as interchangeable routing variables. A semantic safety prior over metadata type, distinguishing symptom/acquisition fields from demographic-only fields, routes between slice-gated transfer and calibration stacking; flipping this semantic prior collapses the gain.

## Remaining novelty risk

Still not fully certified:

- The search was targeted but not exhaustive.
- Direct collisions may exist under terminology such as:
  - context-aware model selection;
  - feature-type-aware adaptation;
  - attribute-guided routing;
  - metadata-gated transfer;
  - safe transfer with clinical covariates.

## Next action

Proceed with one more systematic local/web audit focused on alternative terminology:

- "attribute-guided routing";
- "covariate-guided model selection";
- "context-aware transfer selection";
- "feature-type-aware domain adaptation";
- "metadata-gated ensemble".

If no direct collision appears there, the semantic router can be treated as a credible candidate ICASSP claim, still with the external-scope limitation of only COUGHVID and TosCOVID.

# 5.42 外部数据集侦察与投稿准备度清单

## 本轮目标

继续推进 CoughKD-ShiftAudit，不等待 Cambridge 数据申请结果。本轮完成：

1. 搜索和整理可接入第二/第三外部 cough target；
2. 补 Virufy manifest 构建脚本；
3. 建立外部数据优先级矩阵；
4. 在论文草稿中加入投稿准备度 checklist 和 related-work 边界措辞。

## 新增脚本

### `scripts/build_virufy_manifest.py`

用途：

> 从本地 clone 的 `virufy/virufy-data` 仓库构建 CoughKD external manifest。

设计：

- 递归查找 `labels.csv`；
- 递归索引 `.wav/.flac/.mp3/.ogg/.webm/.m4a`；
- 自动推断常见 audio filename 列；
- 自动推断 common COVID/PCR label 列；
- 映射到 `covid_positive` / `healthy`；
- 输出标准 CoughKD manifest。

命令：

```powershell
cd D:\CoughKD\datasets
D:\CoughKD\tools\mingit\cmd\git.exe -c http.sslBackend=schannel clone https://github.com/virufy/virufy-data.git virufy-data

cd D:\CoughKD\AAAI
D:\conda\envs\CoughKD\python.exe scripts\build_virufy_manifest.py `
  --dataset-dir D:\CoughKD\datasets\virufy-data `
  --out manifests\virufy_external.csv

scripts\run_external_target_guard_audit_windows.cmd manifests\virufy_external.csv virufy
```

验证：

```powershell
D:\conda\envs\CoughKD\python.exe scripts\build_virufy_manifest.py --help
```

已通过。

## 新增数据集优先级矩阵

新增：

- `docs/external_dataset_priority_matrix.md`

优先级：

| Priority | Dataset | Role |
|---:|---|---|
| 1 | Cambridge COVID-19 Sounds / ComParE CCS | 最佳第二独立 external target |
| 2 | Virufy COVID cough data | 快速第三 stress target / sanity external |
| 3 | DiCOVA Track-1 | Coswara-derived auxiliary protocol |
| 4 | CODA TB cough dataset | non-COVID cough transfer target |
| 5 | wet/dry cough datasets | generic cough deployment reliability |

当前建议：

> 立刻申请 Cambridge；同时尝试 Virufy 作为快速 stress target。

## 更新论文草稿

更新：

- `docs/paper_draft_shift_audit.md`

新增：

1. Submission Readiness Checklist；
2. Evidence Threshold for Strong Claim；
3. Related-Work Boundary Wording；
4. 禁用措辞列表。

核心边界：

- 不说 first cough KD；
- 不说 SOTA COVID cough diagnosis；
- 不说 clinical reliability；
- 说 deployment reliability audit / failure cartography。

## 当前投稿准备度

| 项目 | 状态 |
|---|---|
| 非临床 claim | ready |
| multi-seed COUGHVID results | ready |
| calibration + efficiency | ready |
| 25-slice stress test | ready |
| bootstrap target subsets | ready |
| protocol figure | draft ready |
| main table | draft ready |
| second external dataset | missing |
| related-work final citations | partial |

## 下一步

1. 尝试获取 Virufy 数据并生成 `manifests/virufy_external.csv`；
2. 若 Virufy 构建成功，运行：

```powershell
scripts\run_external_target_guard_audit_windows.cmd manifests\virufy_external.csv virufy
```

3. 更新 multi-target audit；
4. 继续推进 Cambridge 数据申请；
5. 若 Virufy 样本太小或结果不稳定，把它写成 stress target，而不是主 external target。


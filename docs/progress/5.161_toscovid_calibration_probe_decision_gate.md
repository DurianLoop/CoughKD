# 5.161 TosCOVID Calibration Probe Decision Gate

Date: 2026-06-16

## Scope

Turn the TosCOVID official-train calibration-budget route into a decision gate. The goal is to avoid spending full-router compute unless a smaller official-train subset shows a real official-test signal.

## Updates

- `scripts/audit_toscovid_official_calibration_budget.py`
  - Adds a `recommendation` field.
  - Recommends `AWAIT_CALIB10_STABLE_SMOKE` when no subset predictions exist.
  - If a ready subset clears 1 pp but not 3 pp, recommends the next larger stable subset.
  - If a ready subset clears 3 pp, recommends full-router escalation.
  - If a ready subset stays below 1 pp, recommends stopping this official-split enhancement route and returning to literature/innovation search.
- `scripts/run_toscovid_official_calibration_probe_windows.cmd`
  - Approval-time driver for a chosen calibration subset and tier.
  - Default: `calib10 stable`.

## Current Recommendation

State: `AWAIT_CALIB10_STABLE_SMOKE`

Next action:

```text
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable
```

This requires 9 calibration prediction files over 500 existing TosCOVID train audio rows. No training and no new data download are involved.

## Gate State

- Calibration-budget gate: `NOT_READY_MISSING_CALIBRATION_PREDICTIONS`
- Submission readiness: `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- Goal completion: `NOT_COMPLETE`

This route remains an approval-bound low-cost probe, not claim-completion evidence.

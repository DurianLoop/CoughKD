# 5.169 Protocol Strength Main-Text Integration

Date: 2026-06-16

Constraint: use existing local artifacts only. No inference, training, or data
download was run.

## Main-text update

Updated `paper_icassp2027/main.tex` to integrate the protocol-strength evidence
from `runs/semantic_router_protocol_strength`:

- The Introduction now states that the contribution is a bounded audit protocol,
  not a new SOTA cough classifier or first KD method.
- The Discussion now states that target confidence/entropy signals are
  insufficient evidence for deployable KD robustness without external
  validation.
- The semantic-router guardrail paragraph now cites the paired comparison
  against the nested target-calibrated controller:
  - COUGHVID: +0.71 pp
  - TosCOVID: +1.90 pp

This strengthens the reviewer-facing boundary:

> semantic routing is not just ordinary target-calibrated model selection.

## Compile and extraction

Recompiled:

```cmd
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

Result:

- `paper_icassp2027/main.pdf`
- 5 pages
- no LaTeX errors
- no overfull boxes reported

Refreshed:

- `paper_icassp2027/main_pdftotext.txt`

MiKTeX/Poppler still prints the known AppData log-permission warning during
`pdftotext`, but extraction completed and the new controller evidence appears in
the text.

## Quality gate update

Updated `scripts/audit_icassp_draft_quality.py`:

- accepts 4 or 5 pages, matching the locally recorded ICASSP 2027 CFP rule:
  4 technical pages plus optional fifth page for references/funding/ethics.

Updated `scripts/audit_semantic_router_submission_readiness.py`:

- replaced the brittle PDF-text anchor `moderate-label rather than few-shot`
  with `30% target calibration remains useful`, because `pdftotext` can split
  the former across lines.

## Verification

Passed:

- `scripts/audit_icassp_draft_quality.py`: `PASS`
- `scripts/audit_semantic_router_claim_consistency.py`: `PASS`
- `scripts/build_semantic_router_claim_dossier.py`: `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- `scripts/audit_semantic_router_goal_completion.py`: `NOT_COMPLETE`
- `scripts/audit_semantic_router_submission_readiness.py`:
  `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`

Current submission-readiness blocker is again only the real empirical-scope
blocker:

- `third_target_ukcovid`: `NOT_READY_AUDIO_AND_PREDICTIONS_MISSING`

The final ICASSP 2027 author kit/template remains a warning.

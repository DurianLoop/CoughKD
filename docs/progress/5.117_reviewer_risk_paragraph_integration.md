# 5.117 Reviewer-risk paragraph integration

## Purpose

5.116 identified reviewer risks for the semantic-constrained transfer router.

This note records a compact Discussion update that states those risks directly in the ICASSP draft.

## File changed

- `paper_icassp2027/main.tex`

## Added Discussion guardrails

Inserted one paragraph after the semantic-router discussion and before the main empirical-scope limitation.

The paragraph states:

1. The semantic router does not guarantee no negative transfer.
   - Split-tail failures remain.
   - Deployment should still monitor target slices.
2. The rule is not intended as a hand-coded shortcut for two datasets.
   - Support comes from inverted, all-slice, no-slice, and paired-bootstrap controls.
   - A third large target is still needed.
3. The evidence is moderate-label, not few-shot.
   - 30% calibration remains useful.
   - 10% calibration fails on COUGHVID.

## Compile check

Command:

```cmd
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
```

Status:

- `pdflatex` succeeded.
- Log scan found no `Undefined`, `Warning`, `Error`, or `Overfull`.
- Output: `main.pdf (5 pages, 233359 bytes)`.
- `pdftotext` updated the text file, with the known MiKTeX user-log permission message.

Page distribution:

| page | extracted chars | notes |
| ---: | ---: | --- |
| 1 | 4469 | abstract/introduction |
| 2 | 5121 | semantic-router method and setup |
| 3 | 5254 | results and semantic tables |
| 4 | 5289 | discussion/conclusion plus references start |
| 5 | 3506 | references |

## PDF text check

`main_pdftotext.txt` contains:

- "The semantic-router claim has three important guardrails."
- "not a guarantee against negative transfer"
- "not intended as a hand-coded shortcut"
- "third large target is needed"
- "moderate-label rather than few-shot"

The text extraction order is affected by LaTeX floats, but the content is present in the PDF.

## Claim status after this pass

This update improves paper defensibility.

Current status:

- local empirical claim: strong;
- paper claim: credible candidate;
- final goal: still not complete, because third-target validation and absolute novelty certainty remain open.

## Next action

The next low-cost action is a final compression/readability pass:

- shorten the abstract if needed;
- decide whether Table 6 should stay in the main paper;
- check whether final ICASSP 2027 page rules allow the current 5-page PDF with references;
- if not, move the label-budget table or some audit details to supplementary material.

# 5.143 Semantic-Router Next-Action Dashboard

Date: 2026-06-09

## Purpose

The semantic-router package now has many gates: paper readiness, claim dossier,
UKCOVID download preflight, archive integrity, extraction preflight, audio
readiness, target readiness, and third-target success. I added a lightweight
dashboard that reads those existing artifacts and reports the single current
next action.

## New script

- `scripts/summarize_semantic_router_next_action.py`

Outputs:

- `runs/semantic_router_next_action/semantic_router_next_action.json`
- `runs/semantic_router_next_action/SEMANTIC_ROUTER_NEXT_ACTION.md`

Current dashboard state:

```text
AWAITING_UKCOVID_DOWNLOAD_APPROVAL
```

Current next action:

```text
Ask the user to approve the resumable UKCOVID audio download (49.95 GiB remaining), then run scripts\run_ukcovid_audio_download_verify_windows.cmd.
```

## Integration

- `scripts/audit_semantic_router_submission_readiness.py` now checks the
  dashboard artifacts.
- `scripts/build_semantic_router_claim_dossier.py` now includes the dashboard
  state in the third-target execution-plan requirement.

## Validation

Commands run:

```text
D:\conda\envs\CoughKD\python.exe -B scripts\summarize_semantic_router_next_action.py
D:\conda\envs\CoughKD\python.exe -B -c "from pathlib import Path; files=['scripts/summarize_semantic_router_next_action.py','scripts/audit_semantic_router_submission_readiness.py','scripts/build_semantic_router_claim_dossier.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('compile_ok', len(files))"
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
D:\conda\envs\CoughKD\python.exe -B scripts\build_semantic_router_claim_dossier.py
```

Result:

- Dashboard generation passed.
- Compile check passed without writing pyc files.
- Submission readiness remains `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`.
- Claim dossier remains `CREDIBLE_CANDIDATE_NOT_FINAL_READY`.
- Remaining hard blocker is unchanged: UKCOVID audio download/extraction,
  onboarding, and subject-grouped third-target validation.

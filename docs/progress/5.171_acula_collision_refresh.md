# 5.171 AcuLa Collision Refresh

Date: 2026-06-16

Constraint: use existing local datasets only. No inference, training, or data
download was run.

## Why this refresh was needed

A new low-cost literature sweep found a stronger adjacent source:

- AcuLa / "Language Models as Semantic Teachers: Post-Training Alignment for
  Medical Audio Understanding"
- URL: `https://arxiv.org/abs/2512.04847`

This paper is close because it uses structured metadata to synthesize clinical
text reports and align medical-audio encoders with language-model semantics.
It also reports cardio-respiratory tasks that include UK COVID-19 and CoughVID.

## Collision assessment

Current non-collision boundary:

- AcuLa is a metadata-to-language representation-alignment / semantic-teacher
  method.
- The local semantic-router claim is a deployment-time transfer-strategy
  selection rule:
  - symptom/acquisition metadata may support slice-gated transfer;
  - demographic-only metadata should route to calibration stacking;
  - inverted, all-slice, and no-slice controls test whether the field semantics
    matter.

Therefore AcuLa is a strong adjacent citation, not a direct replacement for the
current field-semantics transfer-router claim.

This still is not absolute proof of novelty. It is a scoped collision refresh
that strengthens the boundary and prevents overclaiming.

## Updated artifacts

Updated:

- `runs/semantic_router_novelty_matrix/SEMANTIC_ROUTER_SOURCE_LEDGER.md`
- `runs/semantic_router_novelty_matrix/SEMANTIC_ROUTER_NOVELTY_MATRIX.md`
- `scripts/build_semantic_router_claim_dossier.py`
- `scripts/audit_semantic_router_goal_completion.py`
- `scripts/summarize_semantic_router_local_only_innovation_queue.py`

The novelty gate now explicitly requires the AcuLa source ledger anchor before
the literature-collision audit can stay current.

## Exact-phrase spot check

The following exact-phrase style queries did not return a direct collision in
the lightweight web sweep:

- `slice-gated` + `cough` + `metadata`
- `calibration stacking` + `cough` + `metadata`
- `metadata field` + `cough` + `transfer` + `router`
- `target metadata` + `safety prior` + `transfer routing`

This is weak supportive evidence only; it should not be phrased as exhaustive
proof that nobody has done the idea.

## Verification

Passed/refreshed:

- `scripts/audit_semantic_router_claim_consistency.py`: `PASS`
- `scripts/summarize_semantic_router_local_only_innovation_queue.py`:
  `LOCAL_ONLY_QUEUE_ACTIVE`
- `scripts/build_semantic_router_claim_dossier.py`:
  `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- `scripts/audit_icassp_draft_quality.py`: `PASS`
- `scripts/audit_semantic_router_goal_completion.py`: `NOT_COMPLETE`
- `scripts/audit_semantic_router_submission_readiness.py`:
  `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`

Current hard blocker:

- `third_target_ukcovid`: `NOT_READY_AUDIO_AND_PREDICTIONS_MISSING`

The active low-cost local branch remains approval-bound:

```cmd
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

# 5.159 TosCOVID Official-Split Execution Gate and Collision Refresh

Date: 2026-06-16

## Scope

Continue the local-dataset-only route without launching approval-required inference. Convert the TosCOVID official-split idea from a readiness note into an executable gate:

- official train split for calibration/model selection
- official test split for final evaluation
- no new data download
- no model training
- no train inference until explicit approval

## Code/Artifact Updates

- Added `scripts/audit_toscovid_official_split_semantic_router.py`
  - Reports `NOT_READY_MISSING_OFFICIAL_SPLIT_PREDICTIONS` while train predictions are absent.
  - Once train predictions exist, computes:
    - official-train best-single control
    - official-train semantic router
    - official-test delta vs source-only
- Added `scripts/run_toscovid_official_train_inference_windows.cmd`
  - Approval-time driver for 4,803 TosCOVID train audio rows.
  - Runs inference only; no training and no new data.
  - Supports `stable` and `full` modes.
- Updated `scripts/evaluate_external_model_set.py`
  - Added `candidate_f_artifact_env_irm_ramp` seed7 checkpoint so the official-train plan matches the existing test method set.
- Updated gate chain:
  - `scripts/audit_semantic_router_submission_readiness.py`
  - `scripts/build_semantic_router_claim_dossier.py`
  - `scripts/audit_semantic_router_goal_completion.py`

## Current Gate State

- `runs/toscovid_official_split_semantic_router/TOSCOVID_OFFICIAL_SPLIT_SEMANTIC_ROUTER.md`: `NOT_READY_MISSING_OFFICIAL_SPLIT_PREDICTIONS`
- Submission readiness: `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- Claim dossier: `CREDIBLE_CANDIDATE_NOT_FINAL_READY`
- Goal completion: `NOT_COMPLETE`

The new official-split route is a paper-rigor upgrade, not a third-target claim. It remains approval-bound because the next step is inference over 4,803 existing TosCOVID train audio rows.

Execution tiers:

- `stable_smoke`: 9 missing train prediction files for `source_only`, `ce`, and `kd`; command mode `stable`.
- `router_full`: 25 missing train prediction files for the full semantic-router method set; command mode `full`.

Recommended next approval if compute is limited: run `scripts\run_toscovid_official_train_inference_windows.cmd stable` first. The official-split gate will report partial stable-smoke results if only that tier is complete, and full router results after `full` is complete.

## Mini Collision Refresh

Checked the closest current web-indexed neighbors again:

- CoughSense, arXiv:2606.02998: multi-dataset cough classification with Whisper/OPERA, symptom conditioning, and domain adaptation.
- GLoRIA, arXiv:2603.02464: metadata-gated low-rank adaptation for dialectal ASR.

Readout: still no direct replacement for the narrow field-semantics transfer-router claim. These sources reinforce the non-claim boundary: do not claim first symptom-conditioned cough model, first metadata-gated audio adaptation, or first metadata-guided transfer.

# 5.155 Mechanism Support and 2026-06-15 Collision Refresh

Date: 2026-06-15

User constraint: keep using local datasets first; do not request new external data or heavy training unless needed.

## Literature refresh

I ran a narrow current-paper collision refresh around:

- metadata-gated audio/speech adaptation;
- symptom-conditioned cough modeling;
- semantic descriptors for domain adaptation;
- metadata-field routing or transfer selection.

Closest refreshed sources:

- GLoRIA, arXiv:2603.02464 / ICASSP 2026: metadata-gated low-rank adaptation for dialectal ASR.
- CoughSense, arXiv:2606.02998: recent multi-dataset cough classification using the Coswara/COUGHVID/Virufy ecosystem, symptom conditioning, Whisper/OPERA-style representations, and domain-adaptation components.
- Yang and Hospedales 2015: semantic descriptors for multi-domain and multi-task learning.
- Kumagai and Iwata 2018: zero-shot domain adaptation without domain semantic descriptors.

Readout: no refreshed source directly replaces the current semantic-router unit. The claim must stay narrow: not first metadata use, not first symptom-conditioned cough modeling, not first metadata-gated audio adaptation, and not first semantic descriptors for domain adaptation. The defensible unit remains metadata field semantics as a safety prior for choosing between slice-gated transfer and calibration stacking.

## Supplement update

Updated `runs/semantic_router_supplement/main.tex` with a new section:

- `Mechanism Support From Existing Runs`

The section adds Table 7 with existing-run support:

- Random-slice negative control: COUGHVID / symptom_resp +4.59 pp, CI [2.01, 6.85], p(delta<=0)=0.025.
- Slice-balanced gate policy: COUGHVID / symptom_resp +4.46 pp, CI [0.00, 6.73], p(delta<=0)=0.041.
- Metadata-slice oracle: COUGHVID / symptom_resp +3.30 pp, label-revealed upper bound only.

These are now framed as mechanism support, not separate novelty claims.

## Novelty ledger update

Updated `runs/semantic_router_novelty_matrix/SEMANTIC_ROUTER_SOURCE_LEDGER.md`:

- checked date changed to 2026-06-15;
- added a short note that the refresh rechecked metadata-gated audio adaptation, symptom-conditioned cough modeling, and semantic descriptor neighbors.

## Verification

- Recompiled `runs/semantic_router_supplement/main.tex` with `pdflatex` until references were stable.
- Refreshed `runs/semantic_router_supplement/main_pdftotext.txt`.
- Rendered page 4 of the supplement PDF and visually checked the new mechanism-support table. It is readable with no overlap or clipping.
- Checked `main.log` for `Overfull`, `undefined references`, and changed-label warnings; none remain after the final compile.

MiKTeX Poppler wrappers still emit the known AppData log permission warning, but `pdfinfo`, `pdftotext`, and `pdftoppm` returned successfully and produced the expected outputs.

## Current status

This improves reviewer-facing support for the current two-target semantic-router candidate. It does not complete the full ICASSP 2027 objective because the claim still lacks a robust non-duplicate third target under the local-dataset-only constraint.

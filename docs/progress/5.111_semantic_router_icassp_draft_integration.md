# 5.111 Semantic router ICASSP draft integration

## Purpose

5.110 recommended a controlled LaTeX integration of the semantic-constrained transfer router into the ICASSP 2027 draft.

This note records the integration and compile status.

## Files changed

- `paper_icassp2027/main.tex`

Supporting generated artifacts already created in 5.110:

- `runs/semantic_router_paper_tables/semantic_router_tables.tex`
- `runs/semantic_router_paper_tables/SEMANTIC_ROUTER_TABLES.md`
- `scripts/build_semantic_router_paper_tables.py`

## Main text integration

Added:

1. Abstract update:
   - keeps the negative KD audit;
   - adds the moderate-label semantic-constrained router result;
   - reports +3.75 / +3.99 AUROC-point mean gains;
   - states the narrow claim as target metadata semantics for safe transfer routing.
2. Introduction contribution update:
   - old contribution: unlabeled target signals can fail;
   - new contribution: target metadata semantics can rescue routing when unlabeled guards and generic slice selection fail.
3. New method section:
   - `Semantic-Constrained Transfer Routing`;
   - defines the safe metadata set and routing rule:
     - symptom/acquisition metadata enables `slice_gate`;
     - demographic-only metadata routes to `logistic_stack`.
4. Experimental setup update:
   - adds the fourth research question about moderate-label semantic routing;
   - clarifies repeated calibration/evaluation splits.
5. Results update:
   - adds the generated semantic-router tables with:
     - negative controls;
     - calibration label budget.
6. Discussion/conclusion update:
   - keeps the audit claim boundary;
   - adds the constructive lesson that metadata field type is a safety prior under moderate target calibration.

## Compile check

Commands:

```cmd
cd D:\CoughKD\AAAI\paper_icassp2027
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
```

Status:

- `pdflatex` succeeded.
- Cross-references resolved after the second run.
- `main.pdf` generated successfully.
- PDF size: 230,347 bytes.
- PDF page count from LaTeX log: 5 pages.

Log scan:

- no `Undefined` references after second compile;
- no `Error`;
- no `Overfull` lines in the scanned log;
- only underfull box warnings remain.

Page-layout note:

- Extracted text indicates references begin on page 4 and continue to page 5.
- This is likely compatible with a 4-page technical body plus references draft shape, but final ICASSP 2027 rules still need to be checked before submission.

## Current paper status

The draft now carries a positive semantic-router candidate rather than only a negative KD audit.

Current strongest paper story:

> Generic KD and label-free guards fail under cough dataset shift, but moderate-label routing can work when constrained by target metadata semantics. Symptom/acquisition fields can define slice gates; demographic-only fields should route to calibration stacking.

Claim boundary:

- not a clinical diagnostic claim;
- not label-free adaptation;
- not few-shot adaptation;
- not generic metadata use;
- not generic negative-transfer avoidance;
- currently validated on COUGHVID and TosCOVID only.

## Remaining risks

1. Novelty is still not fully certified.
   - 5.110 found no direct collision, but the review must continue against metadata-conditioned model selection and safe-transfer literature.
2. External scope remains limited.
   - UKCOVID/Cambridge/HeAR would materially strengthen the ICASSP claim.
3. The draft may need compression.
   - It compiles to 5 pages including references.
   - If final ICASSP 2027 rules require stricter layout, the semantic budget table or older stress table may need to move to supplementary material.

## Next action

Run a focused paper-quality pass:

- verify table numbering/order in the rendered PDF;
- decide whether to keep both semantic tables in the main paper;
- update title if the paper pivots from pure audit to audit-plus-router;
- continue targeted novelty search before treating the claim as final.

# 5.121 Semantic-router supplement bundle

## Purpose

Continue moving the ICASSP 2027 package toward a defensible submission without starting a large external-data or compute step.

This round creates a compact supplementary PDF that consolidates:

- semantic-router negative controls;
- calibration label-budget results;
- paired bootstrap against semantic controls;
- paired bootstrap against the previous nested controller;
- novelty/collision boundary matrix.

## Files added

- `runs/semantic_router_supplement/main.tex`
- `runs/semantic_router_supplement/main.pdf`
- `runs/semantic_router_supplement/main_pdftotext.txt`

The supplement imports existing generated tables where possible:

- `runs/semantic_router_paper_tables/semantic_router_tables.tex`
- `runs/semantic_router_novelty_matrix/semantic_router_novelty_matrix.tex`

Manual tables were added in the supplement for:

- semantic rule vs inverted/all-slice/no-slice paired bootstrap;
- semantic router vs nested target-calibrated controller paired bootstrap.

## Content

Sections:

1. Scope.
2. Semantic controls and label budget.
3. Paired bootstrap against semantic controls.
4. Comparison with nested controller.
5. Novelty boundary matrix.
6. Current claim boundary.

The current claim boundary in the supplement remains:

> target metadata semantics can act as a safety prior for moderate-label transfer routing under cough dataset shift.

The supplement explicitly avoids claiming:

- first metadata use;
- first symptom-conditioned cough model;
- first safe-transfer method;
- first metadata-gated audio adaptation;
- clinical diagnostic utility.

## Compile check

Commands:

```cmd
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
```

Status:

- Final output: `main.pdf (4 pages, 110094 bytes)`.
- Log scan found no `Undefined`, `LaTeX Warning`, `Package Warning`, `Error`, or `Overfull`.
- Remaining messages are underfull layout notices from the novelty matrix table.
- `pdftotext` updated `main_pdftotext.txt` with the known MiKTeX user-log permission message.

PDF text check:

- "Semantic Controls and Label Budget"
- "Paired Bootstrap Against Semantic Controls"
- "Novelty Boundary Matrix"
- `3.75`
- `3.99`
- "CoughSense"
- "GLoRIA"
- "third large external target"

Page character distribution:

| page | extracted chars |
| ---: | ---: |
| 1 | 1447 |
| 2 | 2186 |
| 3 | 847 |
| 4 | 3283 |

## Notes

The supplement is a support artifact. It does not replace the need for:

- a third large target validation;
- final ICASSP 2027 author-kit confirmation;
- continued novelty watch for late 2026/early 2027 adjacent papers.

## Next best action

Low-cost path:

- add a one-line pointer to the supplement in the internal README or submission checklist;
- keep the main paper unchanged unless the official 2027 kit or a new collision forces changes.

Higher-evidence path:

- request approval for UKCOVID/Cambridge/HeAR-style third-target validation.

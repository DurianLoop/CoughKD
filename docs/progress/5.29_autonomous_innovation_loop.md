# 2026-05-29 Autonomous Innovation Loop

## Verdict

- Final decision: `PIVOT_TO_ANALYSIS_PAPER`
- Baseline reference: `source_only`
- Passing candidates: `[]`

## Task Status

| Task | Status |
|---|---|
| `stage1_panns_seed7` | `skipped` |
| `external_ce_seed7` | `skipped` |
| `external_kd_seed7` | `skipped` |
| `probe_stage1_seed7` | `skipped` |
| `stage1_panns_seed11` | `skipped` |
| `external_ce_seed11` | `skipped` |
| `external_kd_seed11` | `skipped` |
| `probe_stage1_seed11` | `skipped` |
| `stage1_panns_seed23` | `skipped` |
| `external_ce_seed23` | `skipped` |
| `external_kd_seed23` | `skipped` |
| `probe_stage1_seed23` | `skipped` |
| `external_source_only_seed7` | `skipped` |
| `external_tcd_very_strong_seed7` | `skipped` |
| `external_tcd_conf035_seed7` | `skipped` |
| `train_source_only_seed11` | `skipped` |
| `external_source_only_seed11` | `skipped` |
| `train_tcd_very_strong_seed11` | `skipped` |
| `external_tcd_very_strong_seed11` | `skipped` |
| `train_tcd_conf035_seed11` | `skipped` |
| `external_tcd_conf035_seed11` | `skipped` |
| `probe_stage3c_seed11` | `skipped` |
| `train_source_only_seed23` | `skipped` |
| `external_source_only_seed23` | `skipped` |
| `train_tcd_very_strong_seed23` | `skipped` |
| `external_tcd_very_strong_seed23` | `skipped` |
| `train_tcd_conf035_seed23` | `skipped` |
| `external_tcd_conf035_seed23` | `skipped` |
| `probe_stage3c_seed23` | `skipped` |
| `weights_candidate_a_seed7` | `skipped` |
| `train_candidate_a_seed7` | `ran` |
| `external_candidate_a_seed7` | `ran` |
| `probe_candidate_a_seed7` | `ran` |
| `weights_candidate_a_seed11` | `ran` |
| `train_candidate_a_seed11` | `ran` |
| `external_candidate_a_seed11` | `ran` |
| `probe_candidate_a_seed11` | `ran` |
| `weights_candidate_a_seed23` | `ran` |
| `train_candidate_a_seed23` | `ran` |
| `external_candidate_a_seed23` | `ran` |
| `probe_candidate_a_seed23` | `ran` |
| `train_candidate_b_seed7` | `ran` |
| `external_candidate_b_seed7` | `ran` |
| `probe_candidate_b_seed7` | `ran` |
| `train_candidate_b_seed11` | `ran` |
| `external_candidate_b_seed11` | `ran` |
| `probe_candidate_b_seed11` | `ran` |
| `train_candidate_b_seed23` | `ran` |
| `external_candidate_b_seed23` | `ran` |
| `probe_candidate_b_seed23` | `ran` |
| `train_candidate_c_seed7` | `ran` |
| `external_candidate_c_seed7` | `ran` |
| `probe_candidate_c_seed7` | `ran` |
| `train_candidate_c_seed11` | `ran` |
| `external_candidate_c_seed11` | `ran` |
| `probe_candidate_c_seed11` | `ran` |
| `train_candidate_c_seed23` | `ran` |
| `external_candidate_c_seed23` | `ran` |
| `probe_candidate_c_seed23` | `ran` |

## Method Summary

| Method | Kind | Ext macro AUROC | Ext COVID AUROC | Ext macro AUPRC | Domain probe AUC | Task probe AUC | n |
|---|---|---:|---:|---:|---:|---:|---:|
| ce | baseline | 0.509645 +/- 0.017228 | 0.512610 +/- 0.042319 | 0.340692 | 0.717707 | 0.620738 | 3 |
| kd | baseline | 0.512258 +/- 0.004770 | 0.533877 +/- 0.015891 | 0.339684 | 0.724281 | 0.618142 | 3 |
| source_only | control | 0.514576 +/- 0.001456 | 0.519089 +/- 0.002273 | 0.342133 | 0.719719 | 0.627669 | 3 |
| tcd_very_strong | current | 0.515071 +/- 0.001343 | 0.521790 +/- 0.002469 | 0.342402 | 0.722093 | 0.626639 | 3 |
| tcd_conf035 | current | 0.515277 +/- 0.001372 | 0.521250 +/- 0.002380 | 0.342553 | 0.721510 | 0.626578 | 3 |
| candidate_a | candidate | 0.511354 +/- 0.007947 | 0.507125 +/- 0.038489 | 0.339012 | 0.717614 | 0.632911 | 3 |
| candidate_b | candidate | 0.512666 +/- 0.004694 | 0.507954 +/- 0.025986 | 0.343312 | 0.717058 | 0.628989 | 3 |
| candidate_c | candidate | 0.512425 +/- 0.002308 | 0.523014 +/- 0.012395 | 0.339565 | 0.719535 | 0.625896 | 3 |

## Innovation Gate

### tcd_very_strong

- Passed: `False`
- external delta macro=+0.000495, covid=+0.002702
- positive seeds=3/3
- probe delta domain=+0.002374, task=-0.001030

### tcd_conf035

- Passed: `False`
- external delta macro=+0.000701, covid=+0.002161
- positive seeds=3/3
- probe delta domain=+0.001791, task=-0.001090

### candidate_a

- Passed: `False`
- external delta macro=-0.003222, covid=-0.011963
- positive seeds=2/3
- probe delta domain=-0.002105, task=+0.005243

### candidate_b

- Passed: `False`
- external delta macro=-0.001910, covid=-0.011135
- positive seeds=1/3
- probe delta domain=-0.002661, task=+0.001321

### candidate_c

- Passed: `False`
- external delta macro=-0.002151, covid=+0.003925
- positive seeds=1/3
- probe delta domain=-0.000184, task=-0.001773

## Interpretation

- If no candidate passes, the controlled conclusion is that current KD variants do not yet provide enough external-generalization and representation evidence for a strong AAAI method claim.
- In that case the safer paper route is an analysis-oriented study on when KD helps or fails for ultra-light cough audio models under dataset shift.
- If a candidate passes, the next stage should expand to DiCOVA, calibration, latency, and teacher/student efficiency tables rather than adding more arbitrary combinations.

## 中文执行结论（修正版）

本轮自动闭环已经按固定队列完成，没有继续做无边界的 teacher x student x KD 穷举。结论是：当前候选方法 A/B/C 均未达到 innovation gate，因此不应包装成“已获得 AAAI 级别方法创新”的 claim。

基准参考采用 `source_only`，其 COUGHVID external macro AUROC 为 `0.514576 +/- 0.001456`，external COVID AUROC 为 `0.519089 +/- 0.002273`。两个当前方法 `tcd_very_strong` 和 `tcd_conf035` 虽然 3/3 seeds 都有很小的 external macro 正收益，但增益只有 `+0.000495` 和 `+0.000701`，没有达到 `+0.003` 的 gate，同时 probe 上 domain AUC 上升、task AUC 略降，因此不能作为稳定创新。

三个候选方法的结果如下：

- Candidate A shortcut-suppressed KD：external macro delta `-0.003222`，COVID delta `-0.011963`，2/3 seeds 正收益；probe 上 domain 降、task 升，但外部泛化不成立。
- Candidate B disagreement-gated KD：external macro delta `-0.001910`，COVID delta `-0.011135`，只有 1/3 seeds 正收益；说明抑制 teacher disagreement 不足以稳定提升跨数据集表现。
- Candidate C probe-adversarial student：external macro delta `-0.002151`，COVID delta `+0.003925`，只有 1/3 seeds 正收益；seed 7 的 probe 有一定机制迹象，但 seed 11 domain AUC 反而上升，整体不稳定。

因此下一步应转为分析型论文路线：**When Does KD Help or Fail for Ultra-Light Cough Audio Models under Dataset Shift?** 核心论点不再是“提出一个显著优于 baseline 的 KD 方法”，而是系统证明：在 Coswara -> COUGHVID 的跨数据集设置中，轻量 cough student 的 KD 增益很弱，probe-level shortcut suppression 与 external generalization 并不必然一致，单靠 teacher response / confidence / adversarial domain suppression 难以形成可靠部署 claim。

## 中文执行结论

本轮自动闭环已经按固定队列完成，没有继续做无边界的 teacher × student × KD 穷举。结论是：当前候选方法 A/B/C 均未达到 innovation gate，因此不应包装成“已获得 AAAI 级别方法创新”的 claim。

基准参考采用 `source_only`，其 COUGHVID external macro AUROC 为 `0.514576 +/- 0.001456`，external COVID AUROC 为 `0.519089 +/- 0.002273`。两个当前方法 `tcd_very_strong` 和 `tcd_conf035` 虽然 3/3 seeds 都有很小的 external macro 正收益，但增益只有 `+0.000495` 和 `+0.000701`，没有达到 `+0.003` 的 gate，同时 probe 上 domain AUC 上升、task AUC 略降，因此不能作为稳定创新。

三个候选方法的结果如下：

- Candidate A shortcut-suppressed KD：external macro delta `-0.003222`，COVID delta `-0.011963`，2/3 seeds 正收益；probe 上 domain 降、task 升，但外部泛化不成立。
- Candidate B disagreement-gated KD：external macro delta `-0.001910`，COVID delta `-0.011135`，只有 1/3 seeds 正收益；说明抑制 teacher disagreement 不足以稳定提升跨数据集表现。
- Candidate C probe-adversarial student：external macro delta `-0.002151`，COVID delta `+0.003925`，只有 1/3 seeds 正收益；seed 7 的 probe 有一定机制迹象，但 seed 11 domain AUC 反而上升，整体不稳定。

因此下一步应转为分析型论文路线：**When Does KD Help or Fail for Ultra-Light Cough Audio Models under Dataset Shift?** 核心论点不再是“提出一个显著优于 baseline 的 KD 方法”，而是系统证明：在 Coswara → COUGHVID 的跨数据集设置中，轻量 cough student 的 KD 增益很弱，probe-level shortcut suppression 与 external generalization 并不必然一致，单靠 teacher response / confidence / adversarial domain suppression 难以形成可靠部署 claim。

## 2026-05-31 追加：Source-relative Guard 复核

本轮继续验证 CoughKD-Guard，但没有继续增加 teacher × student × KD 的开放式穷举，而是只检查一个更窄的假设：方法相对于 `source_only` 的目标域预测漂移，能否作为无标签目标域下选择 KD 方法的信号。

新增并复跑的文件：

- `scripts/summarize_coughvid_slice_guard.py`
- `scripts/learn_coughkd_guard_from_slices.py`
- `docs/progress/5.34_source_relative_guard_audit.md`

新增 source-relative 信号包括：`agree_source_only`、`l1_to_source_only`、`confidence_gap_source_only`、`entropy_gap_source_only`。

结果摘要：

| 审计 | 关键结果 |
|---|---|
| 固定 slice guard | 仍然全部选择 `tcd_conf035`，`age_old` slice 发生负迁移 |
| source-relative 相关性 | `l1_to_source_only` 与收益负相关，Pearson `-0.322147`，Spearman `-0.387529` |
| LOSO learned guard | negative selected `2/9`，mean selected macro delta `-0.000414` |
| best post-hoc 上限 | mean best post-hoc macro delta `0.003096` |
| vanilla KD | mean KD macro delta `-0.001185` |

判断：当前 source-relative CoughKD-Guard 不能作为方法创新 claim。它提供了一个可解释的失败分析信号，即“相对 source-only 漂移过大可能对应风险”，但这个信号不足以稳定选择方法，也不能避免负迁移。

因此，当前自动闭环继续保持原结论：A/B/C 以及后续 source-relative guard 都没有达到 innovation gate。下一步不再强行包装方法，而是转向分析型论文路线：**When Does KD Help or Fail for Ultra-Light Cough Audio Models under Dataset Shift?**

## 2026-05-31 追加：分析型路线证据包

新增脚本：

- `scripts/summarize_kd_failure_analysis.py`
- `scripts/summarize_calibration_efficiency.py`

新增报告：

- `runs/kd_failure_analysis/KD_FAILURE_ANALYSIS_REPORT.md`
- `runs/calibration_efficiency/CALIBRATION_EFFICIENCY_REPORT.md`
- `docs/progress/5.35_analysis_route_with_calibration_efficiency.md`

关键结论：

- 当前最好方法 `tcd_conf035` 相比 `source_only` 只有 `+0.000701` external macro AUROC，不达方法创新 gate。
- 固定 slice guard 有 `1/9` 负迁移，LOSO learned guard 有 `2/9` 负迁移，不能作为 label-free selection 方法 claim。
- 校准指标和外部 AUROC 不一致：candidate A/B 的 ECE 更低，但 external macro AUROC 更差。
- 部署效率明确成立：student `20,717` 参数，teacher `81,043,476` 参数，约 `3911.93x` 参数压缩；student CPU latency 约 `1.126 ms`。

当前路线判断：不再继续无边界方法搜索，优先补独立外部数据集和分析型论文图表。

## 2026-05-31 追加：25-slice stress test

本轮把 COUGHVID slice stress test 从 9 个 metadata-defined slices 扩展到 25 个 slices，包括 cough_detected 四分位、age 四分位以及 sex/age 与 respiratory_condition 的交叉 slice。

新增/更新：

- `scripts/summarize_coughvid_slice_guard.py`
- `docs/progress/5.37_expanded_slice_stress_test.md`

复跑结果：

- fixed guard negative selected: `6/25`
- fixed guard mean selected macro delta: `0.000484`
- fixed guard best post-hoc mean delta: `0.008675`
- LOSO negative selected: `7/25`
- LOSO mean selected delta: `-0.000201`
- always vanilla KD mean delta: `-0.000190`

判断：扩展 slice 后，post-hoc best 的局部收益更明显，但 target-unlabeled guard 更不能稳定抓住这些收益。这进一步支持 `CoughKD-FailBench / failure cartography` 路线，而不是方法型 claim。

## 2026-05-31 追加：bootstrap target-subset stability

新增 `scripts/bootstrap_coughvid_method_stability.py`，从 COUGHVID test set 中抽取 200 个目标子集，每个子集 800 条样本，用已有 predictions 审计各方法的 macro AUROC delta 稳定性。

输出：

- `runs/coughvid_bootstrap_stability/BOOTSTRAP_STABILITY_REPORT.md`
- `runs/kd_failure_analysis/figures/bootstrap_delta_distribution.pdf/png`
- `docs/progress/5.38_bootstrap_target_subset_stability.md`

关键结果：

- `tcd_conf035`: mean delta `+0.000823`，positive rate `0.74`，best-posthoc rate `0.215`。
- vanilla KD: mean delta `-0.001265`，negative rate `0.535`。
- candidate A/B/C 方差更大，说明 shortcut-aware/disagreement-aware 方法存在 target-composition sensitivity。

判断：这进一步支持 CoughKD-FailBench/failure cartography，而不是强方法 claim。

## 2026-05-31 追加：CoughKD-ShiftAudit paper skeleton

本轮继续收缩 claim，并把 `CoughKD-FailBench` 改名为 `CoughKD-ShiftAudit`，避免与已有 FailBench 命名冲突，同时更准确表达 dataset shift audit。

新增/更新：

- `scripts/build_failbench_main_table.py`
- `scripts/bootstrap_coughvid_method_stability.py` 支持 `--out`，避免不同 subset size 覆盖结果
- `scripts/summarize_bootstrap_sensitivity.py`
- `docs/progress/5.39_novelty_matrix_and_name_pivot.md`
- `docs/progress/5.40_icasSP2027_paper_skeleton.md`

新增输出：

- `runs/kd_failure_analysis/SHIFT_AUDIT_MAIN_TABLE.md`
- `runs/kd_failure_analysis/shift_audit_main_table.csv`
- `runs/coughvid_bootstrap_sensitivity/BOOTSTRAP_SENSITIVITY.md`

Bootstrap sensitivity 结论：`tcd_conf035` 在 subset size 400/800/1200 下都是最稳定正收益方法，但 mean delta 只有约 `+0.0007` 到 `+0.0009`，仍然不能作为强方法 claim。

当前 Go/No-Go：方向更稳，但仍不是投稿完成；必须争取第二外部目标数据集。

## 2026-05-31 追加：ShiftAudit protocol + paper draft

新增：

- `scripts/draw_shift_audit_protocol.py`
- `runs/kd_failure_analysis/figures/shift_audit_protocol.pdf/png`
- `docs/external_data_request_pack.md`
- `docs/paper_draft_shift_audit.md`
- `docs/progress/5.41_shift_audit_protocol_and_paper_draft.md`

当前判断：CoughKD-ShiftAudit 可以作为 ICASSP 分析型论文方向继续推进，但仍是 Conditional Go。没有第二外部目标数据集时，不能宣称已经达到 main-track 投稿强度。

## 2026-05-31 追加：external dataset scouting

新增：

- `scripts/build_virufy_manifest.py`
- `docs/external_dataset_priority_matrix.md`
- `docs/progress/5.42_external_dataset_scouting_and_readiness.md`

更新：

- `docs/paper_draft_shift_audit.md` 加入 Submission Readiness Checklist 和 related-work boundary wording。

判断：Cambridge/ComParE 仍是最优第二独立外部目标；Virufy 是最快可尝试的公开 stress target；DiCOVA 只能作为 Coswara-derived auxiliary protocol。

## 2026-05-31 追加：Virufy tiny external stress target

新增并完成 Virufy tiny stress target 接入。使用 openssl + 127.0.0.1:7897 proxy 成功 clone `virufy/virufy-data` 到 `D:\CoughKD\datasets\virufy-data-openssl`。

新增/更新：

- `scripts/build_virufy_manifest.py`：加入 `corona_test` 标签列支持
- `manifests/virufy_external.csv`：16 条 clinical cough，healthy 9 / covid_positive 7
- `scripts/build_multitarget_shift_audit_table.py`
- `runs/kd_failure_analysis/SHIFT_AUDIT_MULTITARGET_TABLE.md`
- `docs/progress/5.43_virufy_tiny_external_stress_target.md`

结果：Virufy 上 source-only macro `0.4762`，post-hoc best 是 CE-only `0.5979`，guard 选择 `tcd_very_strong` 且 macro delta `-0.026455`。由于 n=16，Virufy 只能作为 tiny stress target，不是强 external validation；但它加强了 target-unlabeled guard 不可靠的 failure evidence。

## 2026-05-31 追加：Virufy segmented stress target

扩展 `scripts/build_virufy_manifest.py`，新增 `--audio-version original|segmented`。构建 `manifests/virufy_segmented_external.csv`，共 121 个 segmented clips，healthy 73 / covid_positive 48，但仍来自 16 个 source recordings/subjects。

完成 `virufyseg` 的 8 methods × 3 seeds 外部评估，并更新 multi-target table。结果：source-only macro `0.5877`，post-hoc best 是 vanilla KD `0.6392`，delta `+0.051465`；guard 选择 `tcd_conf035`，delta 只有 `+0.003092`。

解释：Virufy segmented 不是独立外部队列，但它增强了 stress-test 证据，即最佳 KD 策略会随 target composition / preprocessing granularity 改变，而当前 target-unlabeled guard 无法可靠追踪。

## 2026-06-01 追加：Virufy segmented subject-level audit

为避免把 Virufy segmented 的 121 个 clips 误当成 121 个独立样本，本轮新增 subject-level 聚合脚本：`scripts/aggregate_external_predictions_by_subject.py`。脚本将每个 `recording_id` 映射回 `subject_id`，对同一 subject 的 class probabilities 取平均，再在 16 个 source subjects 上重新计算 macro AUROC / COVID AUROC / macro AUPRC。

关键结果写入：

- `runs/subject_aggregate_virufyseg/SUBJECT_AGGREGATE_REPORT.md`
- `runs/subject_aggregate_virufyseg/method_summary.csv`
- `docs/progress/5.44_virufyseg_subject_level_audit.md`

subject-level 结果：source-only macro `0.764550`；post-hoc best 变成 `candidate_c`，macro `0.854497`，delta `+0.089947`；vanilla KD 仍为正，macro `0.812169`，delta `+0.047619`；guard 对应的 `tcd_conf035` 只有 `+0.007937`。

判断：这不是强方法 claim，因为有效独立样本数只有 16；但它加强了 ShiftAudit 的分析型论点：KD 结论会随 target composition、preprocessing granularity 和 evaluation unit 变化，当前 target-unlabeled guard 不能可靠选择最佳蒸馏策略。

## 2026-06-01 追加：external dataset access re-check

重新核实 Cambridge / ComParE 与 DiCOVA 的数据可获得性，并写入 `docs/progress/5.45_external_access_recheck.md` 与 `docs/external_dataset_priority_matrix.md`。

判断：Cambridge COVID-19 Sounds / ComParE CCS 仍然是最高优先级第二外部目标，但需要 formal Data Access / Data Transfer Agreement；DiCOVA 可以作为 AUC challenge-style auxiliary protocol，但因 Coswara-related，不能替代独立 external validation，且需要 overlap control。

下一步不再扩展无边界 KD 组合，而是优先推进 Cambridge 数据申请，同时整理当前 COUGHVID + Virufy stress target 的 paper-ready 图表和结果表。

## 2026-06-01 追加：paper-ready tables and claim box

新增 `scripts/build_shift_audit_paper_tables.py`，自动生成论文可用的 Markdown / LaTeX 表格：

- `runs/kd_failure_analysis/paper_ready_tables/paper_ready_tables.md`
- `runs/kd_failure_analysis/paper_ready_tables/paper_ready_tables.tex`
- `runs/kd_failure_analysis/paper_ready_tables/claim_box.md`

当前正文主表建议使用两个：

1. COUGHVID external audit table：展示 source-only、vanilla KD、TCD、shortcut-suppressed、disagreement-gated、probe-adversarial 的 external AUROC、AUPRC、ECE、probe 和模型大小。
2. Target/evaluation-unit stress table：展示 COUGHVID、Virufy original、Virufy segmented clip-level、Virufy segmented subject-level 的 post-hoc best 与 guard outcome。

判断：当前最稳 claim 是 CoughKD-ShiftAudit 作为 failure-oriented deployment audit protocol，而不是一个稳定优于 baseline 的新 KD 方法。

## 2026-06-01 追加：2026 literature re-check and claim boundary

新增 `docs/progress/5.47_2026_literature_recheck_and_claim_boundary.md`，重新核对 ShiftKD、Interspeech 2025 respiratory KD、Coughprint、OPERA 和 DiCOVA 等相关工作。

结论：我们的 novelty boundary 必须收窄。不能 claim first KD under shift、first respiratory KD、first cough representation distillation、first respiratory foundation benchmark，也不能 claim 新方法稳定 SOTA。当前最稳贡献是 CoughKD-ShiftAudit：面向 ultra-light cough audio distillation under dataset shift 的 deployment failure audit protocol。

已同步更新 `docs/paper_draft_shift_audit.md`，新增 “Claim Boundary After 2026 Re-check” 小节。

## 2026-06-01 追加：manifest overlap-control audit

新增 `scripts/audit_manifest_overlap.py`，用于所有新外部/辅助目标接入前的 overlap-control。该脚本检查 recording_id、subject_id、path、path stem、path tail、identifier token，并可选启用 SHA-256 音频文件哈希。

已完成 smoke audit：

- Coswara vs Virufy original：5491 source rows / 16 target rows / 0 matches；
- Coswara vs Virufy segmented：5491 source rows / 121 target rows / 0 matches。

输出：

- `runs/overlap_audit/coswara_vs_virufy/OVERLAP_AUDIT.md`
- `runs/overlap_audit/coswara_vs_virufyseg/OVERLAP_AUDIT.md`
- `docs/progress/5.48_manifest_overlap_control_for_auxiliary_targets.md`

判断：这个步骤把 DiCOVA 的 caveat 变成可执行审计流程。后续如果拿到 DiCOVA，必须先过 overlap audit，才能决定它是 auxiliary protocol 还是可报告的去重外部测试。

## 2026-06-01 追加：ShiftAudit LaTeX skeleton

将 `paper/` 目录从旧的 CoughKD 方法论文计划稿改为当前 CoughKD-ShiftAudit 分析型稿件骨架。新的 `paper/main.tex` 直接接入：

- `runs/kd_failure_analysis/paper_ready_tables/paper_ready_tables.tex`
- `runs/kd_failure_analysis/figures/shift_audit_protocol.pdf`
- `external_macro_vs_ece.pdf`
- `external_macro_vs_domain_probe.pdf`
- `slice_delta_gap.pdf`
- `bootstrap_delta_distribution.pdf`

已完成 LaTeX 编译：`pdflatex -> bibtex -> pdflatex -> pdflatex`，成功生成 `paper/main.pdf`。引用 key 检查通过：`missing_cites=[]`。当前仍有少量 overfull hbox warning，正式投稿前随 ICASSP 模板统一调整。

新增进展文档：`docs/progress/5.49_shift_audit_latex_skeleton.md`。

## 2026-06-01 追加：public external target scouting

继续检索可作为第二外部目标的数据集。结论：Cambridge / ComParE 仍然是最高优先级；CODA TB 是最有价值的非 COVID disease-transfer target，但需要 Synapse/data access；FluSense/HF respiratory event audio 公开但任务是 cough/sneeze/speech/noise event，不是疾病筛查，只能用于 generic cough/audio deployment reliability stress test。

新增 `scripts/build_hf_audio_manifest.py`，用于未来从 Hugging Face audio dataset 抽样保存 wav 并生成 CoughKD-style manifest。已通过 AST 语法检查。更新 `docs/external_dataset_priority_matrix.md` 和新增 `docs/progress/5.50_public_external_target_scouting.md`。

判断：不要用 mixed-source COVID cough mirrors 做主外部验证，因为 overlap/leakage 风险高。下一步仍然优先 Cambridge，备选 CODA TB，FluSense 只作为任务重构后的低优先级 stress target。

## 2026-06-01 追加：submission readiness audit

新增 `scripts/audit_submission_readiness.py`，自动检查当前项目是否达到 ICASSP-strength claim。脚本检查 paper PDF、paper-ready tables、main/multitarget tables、claim box、calibration/efficiency、slice audit、bootstrap stress test、figures、overlap audit、多外部目标状态和 method claim 是否成立。

当前运行结果：

- verdict: `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`
- required evidence files: `15/15`
- main/candidate external targets: `1`
- stress-only targets: `2`
- best COUGHVID method: `tcd_conf035`
- best delta vs source-only: `+0.0007006`
- method-superiority claim: not supported

输出：

- `runs/submission_readiness/SUBMISSION_READINESS.md`
- `runs/submission_readiness/readiness_summary.json`
- `docs/progress/5.51_submission_readiness_audit.md`

判断：当前证据包完整度已经不错，但最终 blocker 很明确：缺第二个大外部目标。下一步必须优先 Cambridge / ComParE，或 CODA TB / DiCOVA auxiliary 路线。

## 2026-06-01 追加：external target onboarding pipeline

新增 `scripts/onboard_external_target.py` 和 `scripts/onboard_external_target_windows.cmd`。给定新 target manifest 后，该流程会按顺序运行 overlap audit、固定模型外部评估、多目标 guard summary、multi-target table、paper-ready tables 和 submission readiness audit。

已用 `manifests/virufy_external.csv` 做 dry-run：展开 24 个 external evaluation commands，并正确列出后续 summary/table/readiness commands；没有实际重跑模型。

新增文档：`docs/progress/5.52_external_target_onboarding_pipeline.md`，并更新 `docs/external_data_request_pack.md`。

判断：拿到 Cambridge / CODA TB / DiCOVA 后，可以直接用 onboarding pipeline 进入完整证据链，而不是手动拼命令。

## 2026-06-01 追加：ICASSP 2027 timeline and action plan

新增 `docs/progress/5.53_icassp2027_timeline_and_action_plan.md`。核实 ICASSP 2027 官网信息：会议时间为 Toronto, 2027-05-16 to 2027-05-21，acceptance notification 显示为 2027-01-13，final paper deadline 显示为 2027-01-27。官网 call 页面当前把 full paper deadline 写成 2027-09-16，与会议和 acceptance 时间冲突，疑似页面 typo 或未校正，因此后续需要持续复核官方 author kit / important dates。

用 ICASSP 2026 author guidelines 作为临时格式参考：regular paper 通常为 4 pages technical content + optional references page。当前 `paper/main.pdf` 是临时 article skeleton，正式投稿前需替换 ICASSP 2027 author kit。

已在 `docs/paper_draft_shift_audit.md` 中加入当前 readiness report 路径和 verdict：`CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`。

## 2026-06-01 追加：第二外部数据访问行动包

当前自动 readiness 重新确认：`CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`，唯一关键 blocker 是缺少第二个大外部目标。因此本轮没有继续做 teacher × student × KD 穷举，而是把下一步转成数据访问和外部目标接入闭环。

新增：

- `docs/data_access/dataset_access_tracker.csv`
- `docs/data_access/data_access_action_pack.md`
- `docs/data_access/README.md`
- `docs/data_access/emails/cambridge_covid19_sounds_request.txt`
- `docs/data_access/emails/dicova_track1_request.txt`
- `docs/progress/5.54_data_access_action_pack.md`

判断：Cambridge COVID-19 Sounds / ComParE CCS 是最高优先级第二独立 COVID cough external target；CODA TB 是非 COVID disease-transfer 备选，会把 claim 扩展成 generic cough deployment reliability；DiCOVA 只能作为 Coswara-derived auxiliary protocol，必须先做 overlap audit。

停止条件保持不变：拿到第二外部目标前，不宣布 A/B/C candidate 创新成立，不把 Virufy n=16 写成强外部验证，也不继续无边界机械组合实验。

## 2026-06-01 追加：2025/2026 literature collision check

本轮继续检索并确认最新相关工作边界。新增进展文件：

- `docs/progress/5.55_2026_literature_collision_and_claim_refinement.md`

关键结论：DNDT/DNDF 2026 已经覆盖强 cross-dataset COVID cough classifier 叙事；CoughViT 2025 覆盖 self-supervised cough representation；Coughprint 2025 覆盖 foundation-to-edge cough representation distillation；ShiftKD 2025 覆盖通用 KD under distribution shift；Interspeech 2025 respiratory KD 覆盖 respiratory sound ensemble soft-label KD。因此当前不能写成 SOTA COVID cough classifier、新 cough representation learner、first cough KD 或 first KD-under-shift。

同步更新：

- `paper/references.bib`：补充/修正 ShiftKD、DNDT/DNDF、CoughViT 条目。
- `paper/sections/02_related_work.tex`：新增 DNDT/DNDF 和 CoughViT 的边界说明。
- 重新编译 `paper/main.pdf` 成功；当前无 undefined citation/error，仍有少量 overfull hbox，后续按 ICASSP 模板再统一处理。

当前最稳 claim 仍然是：`CoughKD-ShiftAudit = bounded, multi-evidence deployment audit for ultra-light cough audio distillation under external dataset shift`。readiness verdict 仍为 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`。

## 2026-06-01 追加：claim-boundary 自动审计

新增 `scripts/audit_claim_boundary.py`，用于检查 paper/draft 是否出现未被否定语境包裹的 over-claim，例如 clinical diagnosis、SOTA、first cough KD、biomarker/causal acoustic signature 等。同时要求文稿中出现 ShiftKD、Coughprint、OPERA、Respiratory KD、DNDT/DNDF、CoughViT、non-clinical boundary 和 second external blocker 等必要边界锚点。

当前运行结果：`PASS`，输出位于 `runs/claim_boundary/CLAIM_BOUNDARY_AUDIT.md`。已将 claim-boundary audit 加入 `scripts/audit_submission_readiness.py` 的 required evidence files。重新运行 readiness 后结论仍为 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`，说明写作边界当前过关，主要 blocker 仍是第二外部目标。

## 2026-06-01 追加：UK COVID-19 Vocal Audio public external candidate

本轮为了解决第二外部目标 blocker，新增公开候选 UK COVID-19 Vocal Audio Dataset open access。该数据集位于 Zenodo，包含 cough/exhalation、participant/audio metadata、train/test splits，标签为 PCR-referenced COVID test information；音频 archive 约 53GB，因此当前策略是先 metadata-only 审计，再决定是否下载音频。

新增脚本：

- `scripts/build_ukcovid_manifest.py`：支持 UK dataset metadata 转 CoughKD manifest，并支持 `--allow-missing-audio`。
- `scripts/download_ukcovid_metadata_windows.cmd`：只下载小型 metadata/split/readme 文件，不下载 53GB 音频。

已用 synthetic metadata 完成 smoke test，输出 `runs/smoke_ukcovid_builder/manifest.csv`；已用 `python -B` + `ast.parse` 完成语法检查。已更新 `docs/external_dataset_priority_matrix.md`、`docs/data_access/data_access_action_pack.md` 和 `docs/data_access/dataset_access_tracker.csv`。当前 readiness 仍然是 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`，因为 UK 音频尚未完成正式评估。

## 2026-06-03 追加：UK metadata manifest and audio download plan

本轮完成 UK COVID-19 Vocal Audio Dataset open access 的 metadata 实质接入：`audio_metadata.csv`、`participant_metadata.csv`、`train_test_splits.csv` 均已下载到 `D:\CoughKD\external\ukcovid_open`。生成了 `manifests/ukcovid_open_external.csv` 和 `manifests/ukcovid_open_test_external.csv`。测试 split manifest 有 22,242 clips / 11,121 subjects，其中 covid_positive 7,640 clips、healthy 14,602 clips。

已完成 Coswara vs UK test manifest overlap audit，结果为 0 matches：`runs/overlap_audit/coswara_vs_ukcovid_open_test/OVERLAP_AUDIT.md`。新增 `scripts/audit_ukcovid_metadata.py` 和 `scripts/prepare_ukcovid_audio_download_plan.py`，并生成 `runs/ukcovid_open_metadata_audit/UKCOVID_METADATA_AUDIT.md` 与 `UKCOVID_AUDIO_DOWNLOAD_PLAN.md`。

Zenodo API 显示音频 archive 为 25 个分卷：`covid_data.z01` 到 `covid_data.z24` 加 `covid_data.zip`，总计 49.95 GiB / 约 53.63 GB。下载脚本为 `runs/ukcovid_open_metadata_audit/download_ukcovid_audio_archive_windows.cmd`，支持续传和跳过完整文件。本轮没有自动启动 53GB 下载。readiness 仍为 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`，但 UK 公开第二外部目标路线已经从“候选”推进到“metadata/manifest/overlap ready，等待音频评估”。

## 2026-06-03 追加：UK audio post-download readiness tooling

本轮继续推进 UK COVID-19 Vocal Audio 外部目标接入。`scripts/build_ukcovid_manifest.py` 新增 `--recursive-audio-search`，用于处理 split zip 解压后 wav 位于子目录的情况。新增 `scripts/check_ukcovid_audio_ready.py`，可检查 25 个 archive 分卷是否完整、22,242 个 test manifest wav 文件是否可在 `D:\CoughKD\external\ukcovid_open` 下递归找到，并在 ready 时输出正式 rebuild + onboarding 命令。

当前检查结果：`NOT_READY_AUDIO_MISSING`，archive 尚未下载，unique audio found 为 0/22,242。已将 `UKCOVID_AUDIO_READY_CHECK.md` 加入 `audit_submission_readiness.py` 的 required evidence。当前 readiness 仍然正确保持为 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL`，直到 UK 音频评估真正完成。

## 2026-06-03 追加：UK post-audio one-command onboarding and literature anchors

新增 `scripts/run_ukcovid_after_audio_windows.cmd`。等 UK 49.95 GiB split audio archive 下载/解压后，该脚本会自动检查 audio readiness、用 `--recursive-audio-search` 重建正式 test manifest、运行 Coswara-vs-UK overlap audit、执行 `onboard_external_target.py --target-tag ukcovid_open`，并刷新 claim-boundary 和 submission readiness 审计。

同时将 UK 数据集论文 Budd et al. Scientific Data 2024 和 Coppock et al. Nature Machine Intelligence 2024 no-evidence/symptom-checker 论文加入 `paper/references.bib` 与 related work。`scripts/audit_claim_boundary.py` 新增 required anchors：`UK COVID-19 Vocal Audio Dataset` 和 `symptom checker`，避免后续写作把 UK 外部评估误包装成临床诊断 claim。
## 2026-06-03 追加：Tos COVID-19 public external candidate

本轮没有继续做 teacher × student × KD 的机械穷举，而是围绕当前 readiness blocker 推进第二外部目标。新增 Buenos Aires 开放数据集 **Tos COVID-19** 作为快速 public external candidate。该数据集通过 CKAN API 可访问，2021 批次元数据字段清楚，标签为 RT-PCR `Detectable` / `No Detectable`，并提供官方 `train/test` subset。

已完成：

- 下载 CKAN package metadata 到 `D:\CoughKD\external\tos_covid19\ckan_package_show.json`。
- 下载 2021 CSV `tos-covid-19.csv`，并生成 metadata-only manifest。
- 下载 2022 CSV `tos-covid-2022.csv`；但 2022 CSV 与 2021 schema 不同，本轮暂不混入正式外部评估。
- 新增 `scripts/build_toscovid_manifest.py`。
- 新增 `scripts/download_toscovid_windows.cmd`。
- 新增 `scripts/run_toscovid_after_audio_windows.cmd`。
- 新增 `docs/progress/5.61_toscovid_public_external_candidate.md`。

2021 official test metadata-only 统计：

| Target | Rows | Healthy | COVID-positive |
|---|---:|---:|---:|
| Tos COVID-19 2021 official test | 499 | 179 | 320 |

当前状态：

- 2021 音频 ZIP 约 60MB，正在下载到 `D:\CoughKD\external\tos_covid19\tos-covid-19.zip`。
- 下载完成后运行 `scripts\run_toscovid_after_audio_windows.cmd`，自动完成解压、manifest、overlap audit、external evaluation 和 readiness 刷新。

阶段判断：

- 若 Tos 外部评估完成，它可以显著缓解 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL` blocker。
- Tos 的论文角色是 independent public external stress target，不是临床诊断有效性证据。
- UK COVID-19 Vocal Audio 仍保留为更大规模 PCR-referenced target，但因 50GiB 音频下载成本较高，不阻塞 Tos 快速验证线。

## 2026-06-03 追加：Tos official-test evaluation completed

Tos COVID-19 2021 official-test 已完成完整闭环：

- 音频 ZIP 已通过 `scripts/download_range_file.py` 分段下载并拼接成功，大小 `60,105,734` bytes。
- 解压后生成 `manifests\toscovid2021_test_external.csv`。
- manifest 含 991 clips，其中 healthy 328、COVID-positive 663。
- Coswara vs Tos overlap audit: 0 matches。
- `scripts\onboard_external_target.py --target-tag toscovid2021_test` 已完成固定模型集外部评估和 summary/readiness 刷新。

Tos 三 seed mean/std：

| Method | Mean macro AUROC | Std |
|---|---:|---:|
| CE | 0.509538 | 0.008707 |
| KD | 0.487960 | 0.012165 |
| Source-only | 0.501291 | 0.001796 |
| TCD very strong | 0.498443 | 0.001956 |
| TCD conf035 | 0.498612 | 0.001951 |
| Candidate A | 0.493776 | 0.011859 |
| Candidate B | 0.488146 | 0.008909 |
| Candidate C | 0.488733 | 0.010924 |

Multi-target audit:

- COUGHVID: best delta vs source-only `+0.000701`；
- Tos COVID-19 2021 test: vanilla KD delta `-0.013331`，guard-selected method delta `-0.002847`；
- Candidate A/B/C 均未达到 innovation gate；
- 自动 readiness verdict 已升级为 `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING`。

结论：

方法型创新 claim 仍不成立；但分析型 CoughKD-ShiftAudit claim 已经具备两个外部 target 的证据基础。停止继续堆 candidate，进入最终写作、ICASSP 模板迁移、claim boundary 和 reviewer-risk polishing。

## 2026-06-03 追加：paper draft synchronized after Tos external target

完成 Tos 评估后，已同步论文草稿与 paper-ready claim：

- 更新 `runs\kd_failure_analysis\paper_ready_tables\claim_box.md`，删除旧的“仍缺第二外部目标”表述。
- 更新 `scripts\build_shift_audit_paper_tables.py`，使 claim box 可重复生成。
- 更新 `paper\main.tex` abstract。
- 更新 `paper\sections\01_introduction.tex`、`04_experiments.tex`、`05_results.tex`、`06_discussion.tex`、`07_conclusion.tex`。
- 新增 `paper\references.bib` 中 `buenosaires2021toscovid` 数据源引用。
- 重新编译 `paper\main.pdf` 成功。

当前自动审计：

- `scripts\audit_claim_boundary.py`: `PASS`
- `scripts\audit_submission_readiness.py`: `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING`
- `paper\main.log`: 无 undefined citation、无 LaTeX error。

当前可写 claim：

> CoughKD-ShiftAudit is a bounded deployment audit showing that ultra-light cough audio distillation under dataset shift is method-sensitive and target-sensitive. Across COUGHVID and Tos COVID-19, common KD/TCD variants provide weak, unstable, or negative external gains; Virufy stress targets further show evaluation-unit sensitivity. This supports an analysis/audit contribution, not a new KD method-superiority or clinical diagnosis claim.

下一步不再继续方法穷举。优先事项：

1. 迁移到 ICASSP 2027 模板并压缩到页数限制；
2. 把 Tos 数据集描述、overlap audit、metadata caveat 写得更严谨；
3. 完善 reviewer-risk 段落：COVID clinical utility、label noise、domain shift、guard failure、small stress target caveat；
4. 若时间允许，再补 UK COVID-19 Vocal Audio 或 Cambridge 数据作为更大规模扩展，而不是当前投稿 blocker。

## 2026-06-03 追加：ICASSP template migration smoke

新增 `paper_icassp2027\` 草稿目录，并用 ICASSP 2026 官方 paper kit 中的 `spconf.sty` 和 `IEEEbib.bst` 作为 ICASSP 2027 临时模板。新增一键编译脚本：

```cmd
cd /d D:\CoughKD\AAAI\paper_icassp2027
compile_windows.cmd
```

当前结果：

- `paper_icassp2027\main.pdf` 可成功生成；
- BibTeX 引用已解析；
- 无 undefined citation；
- 无 LaTeX error；
- 当前 PDF 为 5 页。

当前不合规点：正文、图表和参考文献仍混在第 4-5 页，尚未达到 ICASSP 常规 “4 页正文 + 第 5 页参考文献/可选声明” 的最终形态。

下一步：进入论文压缩，优先保留一张主结果表、一张 target/unit stress table、一张 protocol figure，其他 stress/slice/bootstrap 图移到 appendix 或补充材料。

## 2026-06-03 追加：ICASSP compact draft compiled

已将 `paper_icassp2027\main.tex` 从长版 `\input{../paper/sections/...}` 改为 ICASSP 专用自包含短稿。当前稿件保留：

- COUGHVID external audit table；
- target/unit stress table；
- deployment efficiency table；
- metric disagreement figure；
- protocol figure；
- 两个独立 public external targets：COUGHVID 和 Tos COVID-19；
- Virufy 只作为 stress target；
- 明确不 claim clinical diagnosis / SOTA / robust KD method superiority。

重新运行：

```cmd
cd /d D:\CoughKD\AAAI\paper_icassp2027
compile_windows.cmd
```

结果：

- `paper_icassp2027\main.pdf` 成功生成；
- PDF 当前 3 页；
- BibTeX 引用已解析；
- 无 undefined citation；
- 无 LaTeX Error；
- 无 Overfull hbox。

项目级审计：

- `scripts\audit_claim_boundary.py`: `PASS`
- `scripts\audit_submission_readiness.py`: `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING`

下一步：利用剩余页数补强正式投稿稿的可读性和审稿防御，例如加入数据集表、loss/guard 公式、Tos/COUGHVID caveat 和 reviewer-risk paragraph；但不再继续无边界方法穷举。

## 2026-06-03 追加：ICASSP 4-page short paper strengthened

本轮没有继续运行新的 candidate，也没有做 teacher × student × KD 穷举；工作集中在把已经成立的分析型 claim 写成更像正式 ICASSP 投稿的短稿。

已完成：

- 在 `paper_icassp2027\main.tex` 中加入数据集/协议表，明确 Coswara 是 source，COUGHVID 和 Tos COVID-19 是 public external targets，Virufy/ Virufy-seg 只作为 stress targets；
- 加入统一 audit loss 公式，说明 CE、vanilla KD、TCD、shortcut/disagreement/probe controls 是被审计对象，而不是各自宣称为原创方法；
- 加入 target-unlabeled guard 公式，明确 guard 使用 target confidence、entropy、predicted label balance 和 source-relative shift，并且只能通过外部标签后的结果判定其有效性；
- 加入 threats-to-validity 段落，限制 claim 边界：不控制症状、人口统计、录音设备或招募渠道，因此不做临床诊断和疾病机制 claim；
- 重新编译 `paper_icassp2027\main.pdf`。

当前验证结果：

| Check | Result |
|---|---|
| PDF compile | PASS |
| PDF pages | 4 |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| Claim-boundary audit | PASS |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

阶段判断：

- 方法型创新 gate 仍然没有通过，不能写成“我们提出了稳定优于 baseline 的新 KD 方法”；
- 分析型/审计型 claim 已经具备两个外部 public targets 和 stress targets 支撑；
- 下一步应进入 paper polish：压缩语言、强化 contribution framing、优化图表标题和 discussion，而不是继续堆组合实验。

## 2026-06-03 追加：contribution framing polish

在 `paper_icassp2027\main.tex` 的 Introduction 中加入了更明确的 contribution framing：

1. bounded audit protocol for ultra-light cough KD under public-dataset shift；
2. multi-evidence results across two external targets and evaluation-unit stress tests；
3. guarded-selection analysis showing that unlabeled target signals can fail even when the student is highly deployable。

这一步的目的不是提高实验数值，而是让审稿人更快理解本文的贡献类型：它不是新的 SOTA classifier，也不是 first KD method，而是一个 failure-oriented deployment audit。

重新验证：

| Check | Result |
|---|---|
| PDF compile | PASS |
| PDF pages | 4 |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| Claim-boundary audit | PASS |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

下一步继续做 paper polish：优先检查表格是否过宽、caption 是否自解释、abstract 是否过度防守，以及 discussion 是否把 failure audit 的价值讲清楚。

## 2026-06-03 追加：paper table readability QA

本轮对 `paper_icassp2027\main.pdf` 做了视觉 QA：

- 使用 `pdftoppm.exe` 将 PDF 转成页面图片；
- 检查第 2 页：数据集表、效率表、loss/guard 公式均可读；
- 检查第 3 页：发现原来的 COUGHVID 主结果表和 target/unit stress 表作为单栏表过小，不利于审稿阅读。

修复：

- 修改 `scripts\build_shift_audit_paper_tables.py`，让两张 paper-ready 主结果表使用 `table*` 和 `\textwidth`；
- 重新生成 `runs\kd_failure_analysis\paper_ready_tables\paper_ready_tables.tex`；
- 重新编译 `paper_icassp2027\main.pdf`；
- 重新生成页面截图检查，第 3 页主表和 stress 表已变成双栏宽表，阅读体验显著改善。

验证：

| Check | Result |
|---|---|
| PDF compile | PASS |
| PDF pages | 4 |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| Visual QA page 2 | PASS |
| Visual QA page 3 | PASS |
| Claim-boundary audit | PASS |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

阶段判断：当前 ICASSP 短稿已经具备可读的核心图表版式。后续优先级从排版修复转为文字精修和参考文献元数据清理。

## 2026-06-03 追加：reference metadata cleanup

本轮清理了 `paper\references.bib` 中几个容易被审稿人注意到的占位式引用：

- `toikkanen2025respiratorykd`：修正作者为 Miika Toikkanen 和 June-Woo Kim，并补充 arXiv 信息；
- `coughprint2025`：补齐作者、期刊、volume、pages、DOI 和 IEEE Xplore 链接；
- `opera2024`：补齐正式题名、作者列表和 arXiv 信息，删除 Anonymous / metadata placeholder。

重新编译 `paper_icassp2027\main.pdf` 后：

| Check | Result |
|---|---|
| PDF compile | PASS |
| PDF pages | 4 |
| `main.bbl` Anonymous / placeholder check | none |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| Claim-boundary audit | PASS |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

阶段判断：参考文献元数据的明显风险已降低。后续需要继续做语言层面的 polish，并在最终投稿前再次按 ICASSP 2027 官方 author kit 复核格式。

## 2026-06-03 追加：2025/2026 literature collision recheck

新增文档：

- `docs\progress\5.63_literature_collision_recheck_and_paper_risk.md`

本轮复核了以下近邻工作对当前 claim 的影响：

| Work | 覆盖了什么 | 对当前 claim 的限制 |
|---|---|---|
| ShiftKD 2025 | 通用 KD under distribution shift benchmark | 不能写 first KD under shift |
| DNDT/DNDF 2026 | COVID cough cross-dataset classifier | 不能写 first cross-dataset COVID cough evaluation 或 SOTA classifier |
| CoughViT 2025 | self-supervised cough representation learning | 不能写新的 cough representation learner |
| Respiratory KD 2025 | respiratory sound architecture-agnostic KD | 不能写 first respiratory KD |
| Coughprint 2025 | cough representation distillation | 不能写 first cough distillation |
| OPERA 2024 | respiratory acoustic foundation models | 不能把 teacher/foundation 表征写成原创 |

结论：

当前 `CoughKD-ShiftAudit` claim 仍然成立，但必须坚持 analysis/audit framing：

> bounded deployment audit / failure cartography for ultra-light cough audio distillation under public-dataset shift.

本轮同步修正了 `paper\references.bib`：

- `shiftkd2025`：更新 volume/pages/DOI；
- `saravanan2026dndt`：更新 volume/pages/DOI；
- 前一轮已补齐 `toikkanen2025respiratorykd`、`coughprint2025`、`opera2024` 元数据。

后续写作原则：

- 保留 “not a new SOTA cough classifier or first KD method”；
- 保留 “loss variants are audit targets rather than novel methods”；
- 不把负结果写成失败，而是写成 deployment reliability audit 的实证发现。

## 2026-06-03 追加：abstract polish after collision check

在完成 2025/2026 文献碰撞检查后，对 `paper_icassp2027\main.tex` 的 abstract 末尾做了轻量重写。

原来的表达偏防守：

> These results support an audit contribution rather than a method-superiority or clinical-diagnosis claim.

现在改成更主动的审计贡献表达：

> The audit shows that efficiency, calibration, and unlabeled target confidence are insufficient evidence for deployable KD robustness without external validation.

这个改动保留了 claim 边界，但让摘要更像一篇 reliability / audit paper，而不是单纯报告负结果。

重新验证：

| Check | Result |
|---|---|
| PDF compile | PASS |
| PDF pages | 4 |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| Claim-boundary audit | PASS |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

下一步：

1. 继续 polish introduction/discussion，让 failure cartography 的价值更清楚；
2. 检查 official ICASSP 2027 author kit，一旦发布就替换当前 ICASSP 2026 临时模板；
3. 如果 UK COVID-19 Vocal Audio 音频下载完成，再接入为更大规模 external target；否则当前 Tos + COUGHVID 已足够支撑 bounded audit claim。

## 2026-06-03 追加：ICASSP official-status check and 2026 risk anchors

新增文档：

- `docs\progress\5.64_icassp_official_status_and_2026_risk_anchors.md`

本轮核对：

- ICASSP 2027 CFP / important dates 官方页；
- 当前 `paper_icassp2027/` 仍应视为 ICASSP-style draft workspace；
- 当前 `spconf.sty` 和 `IEEEbib.bst` 来自 ICASSP 2026 author kit，投稿前必须用 ICASSP 2027 official author kit 复核/替换；
- 官方 CFP 页的 full paper deadline 显示为 `16 September 2027`，与会议 2027 年 5 月和 acceptance/final paper 时间线冲突，疑似页面 typo 或尚未校正，后续必须再次核对。

新增 2026 风险锚点：

| Work | Why it matters |
|---|---|
| Zimmer et al., Annals of Epidemiology 2026 | Lima/Montreal COVID cough cohort 跨队列泛化脆弱，进一步支持 external validation 必要性 |
| Kafentzis and Selisios, Sensors 2026 | TB cough screening 强调 clinical variables、uncertainty、subject/cougher split，支持 broader cough deployment reliability 背景 |

同步改动：

- `paper\references.bib` 新增 `zimmer2026coughacoustic` 和 `kafentzis2026tb`；
- `paper_icassp2027\main.tex` introduction 引用 Zimmer et al. 2026；
- `paper_icassp2027\main.tex` conclusion 引用 Kafentzis and Selisios 2026；
- `paper_icassp2027\README.md` 明确当前是 ICASSP-style draft，不是 final format-verified ICASSP 2027 submission package。

当前 claim 更稳但仍保持边界：

> Efficiency, calibration, and unlabeled target confidence are insufficient evidence for deployable KD robustness without external validation.

本轮最终验证：

| Check | Result |
|---|---|
| PDF compile | PASS |
| PDF pages | 4 |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| New 2026 risk-anchor citations in `main.bbl` | yes |
| Anonymous / placeholder check | none found |
| Claim-boundary audit | PASS |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

## 2026-06-03 追加：Introduction/Discussion contribution polish

本轮没有新增实验，也没有扩展 teacher/student/KD 组合，而是继续把 ICASSP 短稿从“结果报告”打磨成“可靠性审计论文”。

修改文件：

- `paper_icassp2027\main.tex`

主要改动：

1. Introduction 中新增更明确的风险动机：

> Distillation can bridge these goals, but it can also compress teacher biases, label noise, or dataset-specific shortcuts into a student that appears deployment-ready.

这句话把本文的问题从“KD 有没有提升 AUROC”提升为“部署型压缩是否会把不可靠知识一起压缩进小模型”。

2. Discussion 改成三条 deployment lessons：

- compression evidence is necessary but not sufficient；
- proxy reliability evidence can disagree with task transfer；
- target-unlabeled model selection is fragile。

这让 negative/weak transfer 不再像失败结果，而是服务于 failure cartography / deployment reliability audit 的核心发现。

重新验证：

| Check | Result |
|---|---|
| PDF compile | PASS |
| PDF pages | 4 |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| Claim-boundary audit | PASS |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

阶段判断：

当前短稿的主线更清楚：不是证明 COVID 咳嗽诊断有效，也不是提出新 KD 算法，而是证明在 ultra-light cough KD deployment 中，效率、校准、domain probe 和 unlabeled target guard 都不能替代真实外部验证。

## 2026-06-03 追加：ICASSP draft quality audit automation

本轮新增自动化质量门：

- `scripts\audit_icassp_draft_quality.py`

目的：

> 每次修改 ICASSP 短稿后，自动检查是否破坏投稿质量、claim 边界或关键证据锚点。

该脚本检查：

- `paper_icassp2027\main.tex`、`main.pdf`、`main.log`、`main.bbl`、`README.md` 是否存在；
- PDF 是否为 4 页；
- final log 是否存在 undefined citation/reference、LaTeX Error、Overfull hbox；
- TeX 是否包含关键贡献锚点：
  - `bounded audit protocol`
  - `not a new SOTA cough classifier or a first KD method`
  - `audit template`
  - `not a diagnostic claim`
  - `three deployment lessons`
  - `insufficient evidence for deployable KD robustness without external validation`
- bibliography 是否包含关键近邻工作：
  - ShiftKD；
  - Coughprint；
  - OPERA；
  - Respiratory KD；
  - DNDT/DNDF；
  - CoughViT；
  - Zimmer 2026；
  - TB cough 2026；
- README 是否明确当前只是 ICASSP-style draft workspace，不是最终 ICASSP 2027 format-verified submission；
- 是否出现未被否定语境包裹的临床诊断、SOTA、first KD、biomarker 等 forbidden claims。

第一次运行时发现两个误报：

- `clinical utility` 出现在 `not about clinical utility` 中；
- `First, external performance...` 被误判为 first KD claim。

已修复脚本规则：

- 增加 `not about` negation marker；
- 收紧 first-KD 正则，只抓真正的 `first KD / novel KD / first respiratory KD` 类表达。

已将该质量门接入：

- `scripts\audit_submission_readiness.py`

当前验证：

| Check | Result |
|---|---|
| `audit_icassp_draft_quality.py` | PASS |
| PDF compile | PASS |
| PDF pages | 4 |
| Undefined citation/reference | none in final log |
| LaTeX Error | none |
| Overfull hbox | none |
| Submission readiness | `GO_FOR_FULL_SHIFT_AUDIT_CLAIM_PENDING_FINAL_WRITING` |

阶段判断：

现在项目有三层自动防线：

1. claim-boundary audit：防止 overclaim；
2. ICASSP draft quality audit：防止短稿质量退化；
3. submission readiness audit：汇总证据链和投稿状态。

# 5.154 Local Existing-Run Rescout

Date: 2026-06-15

User constraint: use the datasets already present in the workspace before requesting any new external data or heavy training.

## What changed

- Added `scripts/summarize_semantic_router_local_existing_run_rescout.py`.
- Generated `runs/semantic_router_local_existing_run_rescout/SEMANTIC_ROUTER_LOCAL_EXISTING_RUN_RESCOUT.md`.
- Generated machine-readable companion JSON at `runs/semantic_router_local_existing_run_rescout/semantic_router_local_existing_run_rescout.json`.

## Verdict

`NO_NEW_LOCAL_CLAIM_FROM_EXISTING_RUNS`

Existing local runs strengthen the current semantic-router candidate, but they do not expose a separate claimable innovation or a robust local third target.

## Evidence kept as support, not new claims

- Random-slice negative control: real `COUGHVID:symptom_resp` gate delta is +4.59 pp, CI [+2.01, +6.85] pp, p(delta<=0)=0.025, while random controls do not reproduce the effect.
- Slice-balanced gate policy: COUGHVID symptom gate overall delta is +4.46 pp, CI [+0.00, +6.73] pp, p(delta<=0)=0.041.
- Metadata-slice oracle: the COUGHVID symptom_resp oracle reaches +3.30 pp vs source, but it is label-revealed and should remain motivation, not a deployable method claim.

## Candidates rejected or downgraded

- Target-calibrated transfer controller: below the 3 pp stability gate or CI crosses zero on both large targets.
- Unlabeled prediction-geometry selector: best selector has negative worst-target delta and no large-target 3 pp gate.
- Selective coverage frontier: full-target gate fails; same-subset controls show selection effects.
- Subject consistency frontier: TosCOVID subject ensemble is near zero; Virufyseg is positive but only 16 subjects and CIs cross zero.
- Local dataset frontier: no non-duplicate local third target is ready; Virufy/Virufyseg remain auxiliary micro-stress evidence only.

## Current action

Keep the claim boundary narrow: target metadata field semantics as a safety prior for moderate-label transfer routing under cough dataset shift. Do not upgrade to a new local claim from existing runs. Continue low-cost local scouting first, and ask before heavy training or new external data acquisition.

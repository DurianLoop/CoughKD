# 5.58 UK COVID-19 Metadata Manifest and Audio Download Plan

日期：2026-06-03

## 本轮目的

继续解决 ICASSP 2027 claim 的最大 blocker：第二个大外部目标。上一轮发现 UK COVID-19 Vocal Audio Dataset open access；本轮完成 metadata 下载、metadata-only manifest、overlap audit 和音频分卷下载计划。

## 已完成

### 1. UK metadata 已下载

本地目录：

```text
D:\CoughKD\external\ukcovid_open
```

文件：

- `audio_metadata.csv`：72,999 rows，23.7 MB
- `participant_metadata.csv`：72,999 rows，11.5 MB
- `train_test_splits.csv`：72,999 rows，7.3 MB
- `README.md`
- `zenodo_record_10043978.json`

README 确认：该数据集是 UK COVID-19 Vocal Audio Dataset open-access edition，包含 volitional coughs 和 exhalations，并连接 SARS-CoV-2 test results。记录说明 70,794/72,999 participants 有 linked PCR results，24,155/25,776 positives 有 linked PCR results。

### 2. metadata-only manifest 已生成

生成命令：

```powershell
D:\conda\envs\CoughKD\python.exe scripts\build_ukcovid_manifest.py `
  --audio-metadata D:\CoughKD\external\ukcovid_open\audio_metadata.csv `
  --participant-metadata D:\CoughKD\external\ukcovid_open\participant_metadata.csv `
  --splits D:\CoughKD\external\ukcovid_open\train_test_splits.csv `
  --dataset-dir D:\CoughKD\external\ukcovid_open `
  --out manifests\ukcovid_open_test_external.csv `
  --allow-missing-audio `
  --keep-split-values test
```

输出：

- `manifests/ukcovid_open_external.csv`
- `manifests/ukcovid_open_test_external.csv`

测试 split manifest：

- clips/rows：22,242
- subjects：11,121
- healthy clips：14,602
- covid_positive clips：7,640
- audio columns：`cough_file_name` + `three_cough_file_name`
- 当前 audio paths existing：0，因为音频 archive 尚未下载/解压

### 3. overlap audit 已通过

命令：

```powershell
D:\conda\envs\CoughKD\python.exe scripts\audit_manifest_overlap.py `
  --source-manifest manifests\coswara_cough.csv `
  --target-manifest manifests\ukcovid_open_test_external.csv `
  --out runs\overlap_audit\coswara_vs_ukcovid_open_test
```

结果：

```text
verdict: no_overlap_detected_by_configured_checks
source rows: 5491
target rows: 22242
matches: 0
```

输出：

- `runs/overlap_audit/coswara_vs_ukcovid_open_test/OVERLAP_AUDIT.md`

### 4. metadata audit 已生成

输出：

- `runs/ukcovid_open_metadata_audit/UKCOVID_METADATA_AUDIT.md`
- `runs/ukcovid_open_metadata_audit/summary.json`

结论：UK dataset 已经是强 public second external candidate，但真正改变 readiness verdict 还需要下载/解压音频并完成模型评估。

### 5. 音频分卷下载计划已生成

Zenodo API 文件清单解析得到：

- `covid_data.z01` 到 `covid_data.z24`
- `covid_data.zip`
- archive files：25
- total size：49.95 GiB，约 53.63 GB 十进制

输出：

- `runs/ukcovid_open_metadata_audit/ukcovid_audio_archive_files.csv`
- `runs/ukcovid_open_metadata_audit/download_ukcovid_audio_archive_windows.cmd`
- `runs/ukcovid_open_metadata_audit/UKCOVID_AUDIO_DOWNLOAD_PLAN.md`

下载命令：

```cmd
cd /d D:\CoughKD\AAAI
set HTTPS_PROXY=http://127.0.0.1:7897
set HTTP_PROXY=http://127.0.0.1:7897
runs\ukcovid_open_metadata_audit\download_ukcovid_audio_archive_windows.cmd D:\CoughKD\external\ukcovid_open
```

该脚本支持断点续传/跳过完整文件。考虑到体积很大，本轮没有自动启动下载。

## 已更新脚本

- `scripts/build_ukcovid_manifest.py`
- `scripts/audit_ukcovid_metadata.py`
- `scripts/prepare_ukcovid_audio_download_plan.py`
- `scripts/download_ukcovid_metadata_windows.cmd`
- `scripts/audit_submission_readiness.py`

`audit_submission_readiness.py` 已把 UK metadata audit 和 UK overlap audit 纳入 required evidence，但不会把 metadata-only target 误算成已完成 external evaluation。

## 当前 readiness

重新运行：

```text
audit_claim_boundary.py -> PASS
audit_submission_readiness.py -> CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL
```

blocker 仍然正确：没有第二个已完成模型评估的大外部目标。

## 下一步

1. 下载 UK 音频分卷 archive。
2. 解压到 `D:\CoughKD\external\ukcovid_open` 下，使 manifest 中的 wav path 可访问。
3. 重新生成正式 manifest，去掉 `--allow-missing-audio`。
4. 运行：

```powershell
D:\conda\envs\CoughKD\python.exe scripts\onboard_external_target.py `
  --manifest manifests\ukcovid_open_test_external.csv `
  --target-tag ukcovid_open `
  --skip-existing `
  --device auto `
  --batch-size 16
```

如果 UK 评估完成，当前 paper readiness 很可能从 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL` 进入真正的 full ShiftAudit claim 判断阶段。

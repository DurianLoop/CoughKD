# 5.145 Visible Supplement Non-Claim Sync

Date: 2026-06-09

## Purpose

After adding the 2026 npj Digital Medicine multimodal metadata neighbor to the
source ledger, I checked whether the visible supplement already carried the same
claim boundary.

The novelty matrix TeX already included Yang et al. (2026), but the supplement
Scope did not explicitly say that the paper is not claiming first respiratory
metadata or multimodal modeling.

## Update

Updated:

- `runs/semantic_router_supplement/main.tex`
- `scripts/audit_semantic_router_claim_consistency.py`

The supplement Scope now says the claim does not include:

```text
first respiratory metadata or multimodal modeling
```

The claim-consistency audit now requires this visible supplement anchor.

## Validation

Commands run:

```text
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_claim_consistency.py
D:\conda\envs\CoughKD\python.exe -B scripts\build_semantic_router_claim_dossier.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
```

Result:

- Supplement PDF rebuilt successfully at 5 pages.
- `main_pdftotext.txt` contains the new visible non-claim.
- Claim consistency remains `PASS`.
- Claim dossier remains `CREDIBLE_CANDIDATE_NOT_FINAL_READY`.
- Submission readiness remains `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`.
- Remaining hard blocker is unchanged: UKCOVID audio download/extraction,
  onboarding, and subject-grouped third-target validation.

Note: `pdftotext` returned success but printed a MiKTeX log-permission warning;
the output text file was updated and manually checked.

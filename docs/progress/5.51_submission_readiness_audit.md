# 5.51 Submission Readiness Audit

## 本轮目的

随着实验、表格、图和论文骨架逐渐完整，需要一个自动化 gate 来判断当前是否已经达到 ICASSP-strength claim，而不是凭主观感觉推进。

## 新增脚本

```text
scripts/audit_submission_readiness.py
```

该脚本检查：

- `paper/main.pdf` 是否存在；
- LaTeX 主表是否存在；
- main table / multi-target table / claim box 是否存在；
- calibration / efficiency / KD failure summary 是否存在；
- COUGHVID slice audit 和 bootstrap stress test 是否存在；
- protocol 和 metric-disagreement figures 是否存在；
- 多外部目标是否达到 ICASSP 级别；
- Virufy 等 target 是否只能算 stress target；
- 现有 overlap audit 是否 clean；
- 当前是否支持 method-superiority claim。

## 运行命令

```powershell
cd D:\CoughKD\AAAI
D:\conda\envs\CoughKD\python.exe scripts\audit_submission_readiness.py
```

输出：

```text
runs/submission_readiness/SUBMISSION_READINESS.md
runs/submission_readiness/readiness_summary.json
```

## 当前审计结果

Verdict:

```text
CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL
```

已通过：

- required evidence files: `15/15`；
- paper PDF exists；
- paper title 已经是 ShiftAudit；
- old method-superiority title 不再出现；
- COUGHVID multi-seed external audit 存在；
- calibration / efficiency / slice / bootstrap / figures / paper-ready tables 齐全；
- Virufy original 和 segmented 的 overlap audit 为 0 match。

核心 blocker：

```text
No second large independent/candidate external target yet.
```

Target 分类：

| Target | n | Strength | Caveat |
|---|---:|---|---|
| COUGHVID | 3655 | main_external | external target |
| Virufy original | 16 | stress_only | tiny original stress target |
| Virufy segmented | 121 clips / 16 subjects | stress_only | segmented stress target |

方法 claim 判断：

- best COUGHVID method: `tcd_conf035`；
- best external macro delta vs source-only: `+0.0007006`；
- method-superiority claim: not supported。

## 当前结论

项目已经有一个比较完整的 audit-paper 证据包，但还没有达到最终 ICASSP main-track 的强 claim。当前最正确的下一步不是继续做小 KD 变体，而是补第二外部目标：

1. 首选 Cambridge COVID-19 Sounds / ComParE CCS；
2. 备选 CODA TB，但需改写为 generic cough deployment reliability；
3. DiCOVA 只能作为 overlap-controlled auxiliary protocol；
4. Virufy 只能作为 stress target，不作为主证据。

## 下一步

每次新增 target 或补完实验后，重新运行：

```powershell
D:\conda\envs\CoughKD\python.exe scripts\audit_submission_readiness.py
```

直到 verdict 从 `CONDITIONAL_GO_AUDIT_PAPER_NEEDS_SECOND_EXTERNAL` 变成可投稿级别。

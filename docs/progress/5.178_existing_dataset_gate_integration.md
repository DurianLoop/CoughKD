# 5.178 Existing-dataset gate integration

This note records the follow-up after adding the existing-dataset continuation
decision gate.

## Purpose

The previous step created a standalone artifact saying the current loop should
use existing local datasets and ask before TosCOVID calib10 inference. This
step wires that artifact into the higher-level audits so it cannot drift outside
the readiness and completion gates.

## Code changes

Updated:

- `scripts/audit_semantic_router_submission_readiness.py`
- `scripts/audit_semantic_router_goal_completion.py`

New readiness checklist item:

- `existing_dataset_decision`

New goal-completion requirement:

- `existing_dataset_constraint`

The goal-completion novelty-current condition now also requires the PulmoVec
source-ledger URL:

- `https://arxiv.org/abs/2603.15688`

This makes demographic metadata stacking an explicit crowded neighbor rather
than an informal note.

## Low-cost latest-neighbor check

Searched current web/arXiv-adjacent terms:

- `2026 "metadata field" "transfer routing" audio cough`
- `2026 "slice-gated" metadata cough transfer`
- `2026 "demographic metadata" stacking respiratory audio HeAR`
- `2026 "metadata-gated" "cough" "domain adaptation"`
- `"target metadata semantics" "transfer"`
- `"metadata field semantics" "transfer"`
- `"field semantics" "metadata" "domain adaptation"`
- `"slice gate" "calibration stacking"`

Readout:

- no direct hit for target metadata semantics / metadata field semantics /
  slice gate plus calibration stacking;
- PulmoVec remains the closest returned demographic-stacking neighbor;
- GLoRIA remains the closest metadata-gated audio adaptation neighbor;
- CoughSense remains the closest symptom-conditioned/domain-adaptive cough
  modeling neighbor.

The current claim remains narrow:

> target metadata fields are not interchangeable transfer variables; symptom or
> acquisition fields may support slice-gated transfer, while demographic-only
> fields route to calibration stacking.

Do not broaden this into first metadata use, first symptom-conditioned cough
modeling, first metadata-gated audio adaptation, or first demographic metadata
stacking.

## Verification

Commands run:

```powershell
D:\conda\envs\CoughKD\python.exe -B scripts\summarize_existing_dataset_continuation_decision.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_goal_completion.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_claim_consistency.py
```

Results:

- existing-dataset decision: `EXISTING_DATASET_DECISION_ACTIVE`
- submission readiness: `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`
- goal completion: `NOT_COMPLETE`
- claim consistency: `PASS`

Readiness now includes:

- `existing_dataset_decision | PASS`

Goal completion now includes:

- `existing_dataset_constraint | PASS`

The hard blockers remain:

- `third_target_validation`
- `claim_upgrade_allowed`

The next compute step is unchanged and still requires explicit user approval:

```powershell
scripts\run_toscovid_official_calibration_probe_windows.cmd calib10 stable yes
```

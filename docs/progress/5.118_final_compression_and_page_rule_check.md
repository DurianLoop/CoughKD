# 5.118 Final compression and page-rule check

## Purpose

The user selected the low-cost final compression/readability pass.

This note records the small ICASSP draft edits, compile status, and current page-rule risk.

## File changed

- `paper_icassp2027/main.tex`

## Text edits

Compressed the semantic-router wording without changing any experimental numbers or table artifacts.

Main changes:

1. Abstract:
   - Replaced the stronger "necessary for safe transfer routing" phrasing with:
     - "target metadata semantics can act as a safety prior for transfer routing under cough dataset shift."
   - Shortened the router description.
2. Introduction:
   - Shortened the contribution boundary paragraph.
   - Kept the novelty boundary against SOTA cough classification, metadata models, metadata-gated adaptation, safe transfer, and KD.
3. Semantic-router method:
   - Tightened the non-claim sentence.
   - Kept the distinction from metadata-gated adaptation and semantic descriptor learning.
4. Results:
   - Rephrased the semantic-router evidence as main effect, paired bootstrap, negative controls, and label-budget boundary.
5. Discussion:
   - Preserved the three guardrails:
     - not a guarantee against negative transfer;
     - not intended as a hand-coded shortcut for two datasets;
     - moderate-label rather than few-shot.

## Compile and PDF checks

Commands:

```cmd
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
pdftotext main.pdf main_pdftotext.txt
```

Status:

- `pdflatex` succeeded twice.
- Output: `main.pdf (5 pages, 233123 bytes)`.
- No `Undefined`, `LaTeX Warning`, `Package Warning`, `Error`, or `Overfull` matches in `main.log`.
- Remaining messages are underfull hbox/vbox layout notices only.
- `pdftotext` updated `main_pdftotext.txt` with the known MiKTeX user-log permission message.

PDF text check:

- "target metadata semantics can act as a safety prior"
- "moderate-label rather than few-shot"
- "+3.75 and +3.99 points"
- "inverted semantic controls collapse"

Page character distribution from `main_pdftotext.txt`:

| page | extracted chars |
| ---: | ---: |
| 1 | 4617 |
| 2 | 5232 |
| 3 | 5698 |
| 4 | 5459 |
| 5 | 2976 |

Page 5 extraction contains references only.

Temporary page-specific extraction files were produced for checking:

- `paper_icassp2027/main_page4.txt`
- `paper_icassp2027/main_page5.txt`

PowerShell could not delete them because Windows denied access. They are non-source check artifacts.

## Page-rule check

Current finding:

- The ICASSP 2027 website is live and has the conference/CFP information.
- I did not find a 2027-specific final author-kit page-limit document in this pass.
- The current 5-page paper is aligned with the ICASSP 2026 official author-guideline convention only if the fifth page is references/funding/ethics only.
  - Source checked: `https://2026.ieeeicassp.org/author-guidelines/`
- The current PDF satisfies that local check: page 5 contains references only.

Risk:

- The final ICASSP 2027 author kit still needs to be checked when published.
- If 2027 changes the page rule, the first low-cost fallback is to move Table 6, the label-budget table, to supplementary material and keep only the main label-budget sentence in the paper.

## Claim status

The paper wording is now more defensible:

- empirical claim remains credible on two public external targets;
- novelty claim remains narrow and control-backed;
- final publication readiness still needs third-target validation or a stronger official-scope argument.

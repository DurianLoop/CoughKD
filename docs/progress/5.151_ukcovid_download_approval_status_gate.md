# 5.151 UKCOVID Download Approval Status Gate

Date: 2026-06-09

## Purpose

The UKCOVID third-target path is now blocked by external approval rather than
missing scripts. I made that state machine-readable so future reports do not
confuse an unapproved large download with an experiment failure.

## Approval attempt

I requested approval to run:

```cmd
scripts\run_ukcovid_audio_download_verify_windows.cmd
```

The approval review timed out again before approval. No UKCOVID audio download
was started.

## New script

- `scripts/record_ukcovid_download_approval_status.py`

Outputs:

- `runs/ukcovid_open_metadata_audit/ukcovid_download_approval_status.json`
- `runs/ukcovid_open_metadata_audit/UKCOVID_DOWNLOAD_APPROVAL_STATUS.md`

Current verdict:

```text
DOWNLOAD_APPROVAL_NOT_GRANTED
```

Current status:

```text
requested_timeout
```

Attempts recorded:

```text
2
```

## Integration

- `scripts/summarize_semantic_router_next_action.py` now includes
  `download_approval` in its gate verdicts and next-action wording.

Current next-action dashboard now says:

```text
Current approval status is requested_timeout; no download has started.
```

## Validation

Commands run:

```text
D:\conda\envs\CoughKD\python.exe -B scripts\record_ukcovid_download_approval_status.py --status requested_timeout --attempts 2 --checked 2026-06-09
D:\conda\envs\CoughKD\python.exe -B scripts\summarize_semantic_router_next_action.py
D:\conda\envs\CoughKD\python.exe -B scripts\summarize_semantic_router_innovation_fallback.py
D:\conda\envs\CoughKD\python.exe -B scripts\build_semantic_router_claim_dossier.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_goal_completion.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
```

Result:

- Next-action dashboard remains `AWAITING_UKCOVID_DOWNLOAD_APPROVAL`.
- Innovation fallback remains `WAIT_FOR_THIRD_TARGET`.
- Claim dossier remains `CREDIBLE_CANDIDATE_NOT_FINAL_READY`.
- Full-goal audit remains `NOT_COMPLETE`.
- Submission readiness remains `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`.
- Remaining hard blocker is unchanged: UKCOVID audio approval/download,
  extraction, onboarding, and subject-grouped third-target validation.

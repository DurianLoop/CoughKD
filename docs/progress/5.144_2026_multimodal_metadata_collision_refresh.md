# 5.144 2026 Multimodal Metadata Collision Refresh

Date: 2026-06-09

## Trigger

The next-action dashboard reports:

```text
AWAITING_UKCOVID_DOWNLOAD_APPROVAL
```

I requested approval to run the UKCOVID download/checksum driver, but the
automatic approval review timed out. No UKCOVID audio download was started.

## Literature refresh

A targeted 2026 collision check surfaced a close respiratory metadata neighbor:

- Yang et al., "A device-invariant multi-modal learning framework for
  respiratory disease classification," npj Digital Medicine, 2026.
- URL: https://www.nature.com/articles/s41746-026-02445-4

This work is close because it uses cough/respiratory audio with demographics,
symptom descriptions, disease descriptions, and device metadata for respiratory
disease classification.

## Non-collision boundary

This source further tightens the current paper boundary:

- do not claim first respiratory metadata model;
- do not claim first multimodal respiratory model;
- do not claim novelty for metadata fusion, symptom descriptions, disease
  descriptions, demographics, or device metadata;
- keep the claim limited to field-semantics-constrained transfer routing:
  target metadata fields are not interchangeable transfer variables, and their
  semantic type can act as a safety prior for choosing between slice-gated
  transfer and calibration stacking.

## Artifact updates

- `runs/semantic_router_novelty_matrix/SEMANTIC_ROUTER_SOURCE_LEDGER.md`
  now includes the npj Digital Medicine 2026 source.
- `scripts/audit_semantic_router_claim_consistency.py` now requires the source
  ledger to contain the npj Digital Medicine 2026 URL.
- `scripts/build_semantic_router_claim_dossier.py` now includes the same source
  in the novelty-boundary requirement.

## Validation

Commands run:

```text
D:\conda\envs\CoughKD\python.exe -B -c "from pathlib import Path; files=['scripts/audit_semantic_router_claim_consistency.py','scripts/build_semantic_router_claim_dossier.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('compile_ok', len(files))"
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_claim_consistency.py
D:\conda\envs\CoughKD\python.exe -B scripts\build_semantic_router_claim_dossier.py
D:\conda\envs\CoughKD\python.exe -B scripts\audit_semantic_router_submission_readiness.py
```

Result:

- Compile check passed.
- Claim consistency remains `PASS`.
- Claim dossier remains `CREDIBLE_CANDIDATE_NOT_FINAL_READY`.
- Submission readiness remains `NOT_READY_THIRD_TARGET_OR_ARTIFACTS_MISSING`.
- Remaining hard blocker is unchanged: UKCOVID audio download/extraction,
  onboarding, and subject-grouped third-target validation.

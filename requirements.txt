# Foundation smoke pipeline currently uses only the Python standard library.
# Future training work will add pinned ML dependencies such as torch, torchaudio,
# numpy, scipy, scikit-learn, and audio model libraries.
